# 06 — BUILD CHECKLIST: DATABASE + BACKEND (giai đoạn hiện tại)

> **PHẠM VI GIAI ĐOẠN NÀY: chỉ Database + Backend.** KHÔNG code Web App, Mobile App, hay AI Service ở giai đoạn này, kể cả khi thấy "tiện" — nếu cần biết Web/Mobile/AI sẽ dùng dữ liệu này thế nào, đọc `00-OVERVIEW.md` để hiểu bối cảnh, nhưng không viết code cho các phần đó.
>
> **Quy tắc làm việc:**
> 1. Làm đúng thứ tự các mục bên dưới, từ trên xuống. Không bỏ qua bước, không làm tắt.
> 2. Sau khi hoàn thành 1 mục, đánh dấu `[x]` vào ô tương ứng trong chính file này, ghi 1 dòng note ngắn (đã tạo gì/quyết định gì), rồi mới sang mục tiếp theo.
> 3. Nếu gặp chi tiết mà spec (`01-DATABASE-SCHEMA.md`, `02-BACKEND-SPEC.md`) chưa nói rõ, tự quyết định hợp lý nhất, **rồi ghi lại quyết định đó vào `07-DECISIONS-LOG.md`** — không âm thầm tự quyết mà không ghi chú.
> 4. Nếu một bước bắt buộc phải sửa/thêm gì đó khác với spec đã cho, dừng lại và hỏi người dùng trước khi đổi, trừ khi là chi tiết nhỏ không ảnh hưởng cấu trúc chung.
>
> **GHI CHÚ KHÔI PHỤC (quan trọng):** Toàn bộ nội dung bên dưới là bản ghi lại công việc THẬT đã hoàn thành ở phiên làm việc trước, nhưng folder code gốc trên máy đã bị mất. File này chỉ còn là TÀI LIỆU MÔ TẢ những gì đã làm — không phải code thật. Khi bắt đầu lại, Claude Code cần VIẾT LẠI TOÀN BỘ CODE từ đầu, nhưng nên bám sát các quyết định/note đã ghi dưới đây để không phải suy nghĩ lại từ đầu (tên bảng, convention, quy tắc RBAC, cấu trúc mock AI...). Trước khi bắt đầu, kiểm tra xem database trên Neon còn tồn tại không — nếu còn, có thể tận dụng lại schema đã có thay vì tạo mới.

---

## Giai đoạn A — Database

- [x] A1. Khởi tạo project Backend (NodeJS + Express), cài Prisma, kết nối PostgreSQL (`.env` với `DATABASE_URL`). Note (viết lại sau khi mất code, 2026-09-11): `npm init`, cài express/cors/dotenv/@prisma/client/bcrypt/jsonwebtoken/multer/zod + devDeps prisma@6.19.3/nodemon/jest/supertest; npm mới chặn install-scripts theo mặc định (`npm warn install-scripts`) — đã `npm install-scripts approve` cho bcrypt/prisma/@prisma/client/@prisma/engines/@parcel/watcher/unrs-resolver để native binding (bcrypt) và Prisma engines build đúng, verify `npx prisma -v` + bcrypt hashSync chạy OK; PostgreSQL 16 chạy qua Docker Compose (`backend/docker-compose.yml`, container `warehouse_postgres`, user/pass/db `warehouse`/`warehouse`/`warehouse_db`, volume đặt tên, đã `docker compose up -d`); `.env` + `.env.example` tạo `DATABASE_URL`, `PORT`, `JWT_SECRET` (random 48-byte hex), `JWT_EXPIRES_IN`; `prisma/schema.prisma` tối thiểu (datasource+generator, chưa có model — model đầy đủ ở A2) để `prisma generate` chạy được; `src/app.js` (Express 5 app + `/health` trả `{success,data,message}`) và `src/server.js` (`prisma.$connect()` rồi listen) test chạy OK qua `node src/server.js` + `curl /health` → 200, route lạ → 404, log đúng "Database connected" + "Server is running on port 5000". Prisma ghim ở v6.19.3 (xem 07-DECISIONS-LOG.md).
- [x] A2. Viết `schema.prisma` đầy đủ theo `01-DATABASE-SCHEMA.md` (toàn bộ bảng ở mục 1–13, kể cả các bảng bổ sung: recovery, stocktake, liquidation, stock_movements). Note (viết lại 2026-09-11): 21/22 bảng (bỏ `equipment_units` — không dùng quản lý theo serial ở giai đoạn này); PK dạng `String @id @default(uuid())`; field snake_case trực tiếp (model PascalCase + `@@map` về đúng tên bảng snake_case trong spec); enum `EntityStatus`/`OrderStatus`/`MovementType`/`ReferenceType` dùng chung; đầy đủ quan hệ (đặt tên `@relation` tường minh ở các cặp model có ≥2 FK trỏ tới cùng 1 bảng: Export/Liquidation→User, TransferOrder→Warehouse, TransferOrderItem→StorageLocation, ItemCategory tự tham chiếu cha-con), unique constraint (`inventory` theo `(item_id, warehouse_id, location_id)`, `storage_locations` theo `(warehouse_id, location_code)`), index theo gợi ý của spec + các cột FK. Chi tiết quyết định field nullable/required/kiểu dữ liệu: xem `07-DECISIONS-LOG.md`. Đã `prisma format` + `prisma validate` pass.
- [x] A3. Chạy migration đầu tiên (`prisma migrate dev`), xác nhận toàn bộ bảng + khóa ngoại + index tạo đúng. Note (viết lại 2026-09-11): migration `20260911095813_init` áp thành công; xác nhận qua `psql` trong container `warehouse_postgres`: 21 bảng nghiệp vụ (+ `_prisma_migrations`), 47 foreign key, 63 index; kiểm tra mẫu bảng `inventory` đúng cấu trúc + ràng buộc unique `(item_id, warehouse_id, location_id)` + đủ FK tới items/warehouses/storage_locations.
- [x] A4. Viết seed script tối thiểu: roles mặc định (admin/warehouse_manager/warehouse_staff/report_viewer), 1 user admin để test đăng nhập. Note (viết lại 2026-09-11): `prisma/seed.js` (chạy bằng `npm run prisma:seed`), dùng `upsert` nên chạy lại nhiều lần an toàn (đã test chạy 2 lần liên tiếp, không lỗi, không tạo trùng). Seed 4 role đúng tên theo `02-BACKEND-SPEC.md` mục 2. Tạo user admin `admin@warehouse.local` / `Admin@123` (mật khẩu hash bằng bcrypt, salt rounds 10). Đã verify qua `psql`: 4 roles + 1 admin user liên kết đúng role `admin`, status `ACTIVE`.

## Giai đoạn B — Nền tảng Backend

- [x] B1. Setup cấu trúc thư mục Backend (routes/controllers/services/middlewares theo convention rõ ràng, ghi lại convention vào `07-DECISIONS-LOG.md`). Note (viết lại 2026-09-11): `src/{routes,controllers,services,middlewares,utils,validators}` đã tạo sẵn từ A1; `prisma/` giữ ở root (không lồng trong `src/`), `tests/` + `postman/` cũng ở root — lý do và chi tiết xem `07-DECISIONS-LOG.md`.
- [x] B2. Middleware xử lý lỗi tập trung + response format chuẩn `{ success, data, message }`. Note (viết lại 2026-09-11): `src/utils/response.js` (`success(res,data,message,statusCode)` / `fail(res,message,statusCode,data)`), `src/utils/ApiError.js` (class kế thừa Error, có `statusCode`), `src/middlewares/errorHandler.js` (ưu tiên `ApiError` → đúng statusCode; bắt lỗi Prisma `P2002`→409 trùng dữ liệu, `P2003`→409 vi phạm ràng buộc tham chiếu, `P2025`→404 không tìm thấy; bắt `ZodError`→400 kèm chi tiết field; còn lại→500 + log ra console), `src/middlewares/notFoundHandler.js` (404 chuẩn). Gắn thứ tự trong `app.js`: routes → `notFoundHandler` → `errorHandler` (error middleware luôn đứng cuối). Express 5 tự forward lỗi throw trong route/async handler ra error middleware, không cần asyncHandler wrapper riêng. Test thực tế: viết script tạm dùng supertest gọi trực tiếp route throw `ApiError(403)` → đúng 403 + message; route throw `Error` thường → 500 + message chung (không lộ stack trace ra response); route không tồn tại → 404 đúng format; xoá script tạm sau khi verify xong (không commit code test rác).
- [x] B3. Auth: `POST /api/auth/login` (JWT), `GET /api/auth/me`, middleware xác thực JWT. Note (viết lại 2026-09-11): `src/services/auth.service.js` (`login` — kiểm tra tồn tại user → status ACTIVE (khác 403 "Tài khoản đã bị khóa") → so khớp bcrypt → ký JWT payload `{user_id, role: role_name}`; `getMe`), `src/validators/auth.validator.js` (zod: email hợp lệ, password không rỗng), `src/controllers/auth.controller.js`, `src/routes/auth.routes.js` (`POST /login` public, `GET /me` qua `authenticate`), `src/middlewares/auth.js` (`authenticate` đọc header `Authorization: Bearer <token>`, verify JWT, gắn `req.user = {user_id, role}`). Mount `/api/auth` trong `app.js` trước `notFoundHandler`. Test thực tế qua server chạy thật + curl: (1) sai mật khẩu → 401 "Email hoặc mật khẩu không đúng"; (2) đăng nhập đúng `admin@warehouse.local`/`Admin@123` → 200, JWT hợp lệ, response không lộ `password_hash`; (3) `/me` kèm token → 200 trả đúng user (không có `password_hash`); (4) `/me` không token → 401 "Thiếu token xác thực"; (5) route lạ `/api/nope` → 404 chuẩn. Cả 5/5 đúng.
- [x] B4. Middleware RBAC theo role, áp dụng thử trên 1 route mẫu để xác nhận hoạt động đúng. Note (viết lại 2026-09-11): `authorize(...roles)` trong `src/middlewares/auth.js` — kiểm tra `req.user.role` nằm trong danh sách role cho phép, 403 nếu không. Verify bằng script tạm (supertest + JWT ký thủ công cho role `admin`/`warehouse_staff`) trên 1 route mẫu `GET /sample-admin-only` gắn `authenticate` + `authorize('admin')`: token admin → 200; token `warehouse_staff` → 403 đúng message; không token → 401 (authenticate chặn trước khi tới authorize). Script tạm đã xoá sau khi verify (không commit code test rác) — RBAC sẽ được verify lại lần nữa trên route thật đầu tiên ở C1 (`/api/users`, chỉ admin).

## Giai đoạn C — CRUD danh mục

- [x] C1. CRUD `/api/users`, `/api/roles` (chỉ Admin). Note (viết lại 2026-09-11): `user.*`/`role.*` (routes/controllers/services/validators), cả 2 route file dùng `authorize('admin')` cho TOÀN BỘ method (kể cả GET). `src/utils/roles.js` gom hằng số `ADMIN_ONLY`/`MANAGE_ROLES`/`STAFF_WRITE_ROLES`/`ALL_ROLES` dùng chung cho mọi route sau này. Users: DELETE = soft delete (status→INACTIVE), password hash bcrypt (saltRounds=10) khi tạo/đổi mật khẩu (PUT không gửi `password` thì giữ nguyên hash cũ), response không lộ `password_hash`. Roles: DELETE = xoá thật (không có cột status), dựa vào FK constraint mặc định (`P2002`/`P2003` đã map sẵn ở B2) để chặn xoá role đang có user. Test thực tế (server thật + curl, tạo 3 user test role staff/manager/viewer bằng chính API rồi login): `GET /api/users` với token staff → 403; `GET /api/roles` với token viewer → 403; `GET /api/users` với token admin → 200; xoá (soft-delete) user staff → login lại → 403 "Tài khoản đã bị khóa"; PUT status=ACTIVE → login lại thành công; xoá role `warehouse_staff` khi còn user staff tham chiếu → 409 (đúng, không cho xoá).
- [x] C2. CRUD `/api/item-categories`, `/api/items`. Note (viết lại 2026-09-11): đọc = 4 role (`authorize(...ALL_ROLES)`); ghi (POST/PUT/DELETE) = `admin`+`warehouse_manager` (`authorize(...MANAGE_ROLES)`) — xem `07-DECISIONS-LOG.md` cho lý do field `category_id` bắt buộc. `items` DELETE = soft delete (status→INACTIVE); `item_categories` DELETE = xoá thật (không có cột status), tự động bị chặn 409 bởi FK `items.category_id` (RESTRICT) nếu còn vật tư thuộc danh mục — đã verify thực tế. `items` hỗ trợ filter `category_id`/`status`/`search` (contains, case-insensitive, trên `item_code` HOẶC `item_name`). Test thực tế: token viewer GET item-categories → 200, POST → 403; token staff POST item-categories/items → 403 (đúng, staff không có quyền ghi 2 resource này); token manager POST cả 2 → 201; xoá category khi còn item tham chiếu → 409.
- [x] C3. CRUD `/api/warehouses`, `/api/storage-locations`. Note (viết lại 2026-09-11): `warehouses` ghi = admin+manager (`MANAGE_ROLES`); `storage-locations` ghi = admin+manager+**staff** (`STAFF_WRITE_ROLES`, vì staff được "cập nhật vị trí lưu trữ" theo `00-OVERVIEW.md`). Cả 2 đọc = 4 role, DELETE = soft delete (status→INACTIVE). `storage-locations` filter theo `warehouse_id`/`status`. Test thực tế: token staff POST `/api/warehouses` → 403 (đúng, staff không được tạo kho); cùng token staff POST `/api/storage-locations` → **201** (đúng, staff được tạo vị trí — khác biệt rõ với warehouses/items/suppliers); token viewer POST storage-locations → 403.
- [x] C4. CRUD `/api/suppliers`. Note (viết lại 2026-09-11): ghi = admin+manager, đọc = 4 role, DELETE = soft delete, hỗ trợ filter `status`/`search` (contains, case-insensitive, trên `supplier_name`). Test thực tế toàn bộ C1-C4 chạy trong 1 script end-to-end (tạo 3 user role thật qua API → login từng người → gọi chéo tất cả resource): token staff GET suppliers → 200, POST → 403; token manager POST → 201. Toàn bộ ma trận RBAC ở trên đều đúng 100% theo kỳ vọng — không có sai lệch nào cần sửa lại. Dữ liệu test (user/category/item/warehouse/location/supplier) đã dọn sạch bằng script cleanup sau khi verify xong (DB quay về đúng baseline: 4 role + 1 admin, 0 bản ghi nghiệp vụ khác).

## Giai đoạn D — Tồn kho (đọc)

- [x] D1. `GET /api/inventory` (filter item/warehouse/location). Note (viết lại 2026-09-11): `src/services/inventory.service.js`, `src/controllers/inventory.controller.js`, `src/routes/inventory.routes.js` — đọc cho cả 4 role (`authorize(...ALL_ROLES)`), không có route ghi (inventory chỉ được thay đổi gián tiếp qua các nghiệp vụ kho ở Giai đoạn E). Filter query `item_id`/`warehouse_id`/`location_id` (kết hợp AND nếu truyền nhiều). Mỗi dòng include sẵn `item` (item_code/item_name/unit/min_stock/max_stock), `warehouse` (warehouse_name), `location` (location_code/location_name) — tránh FE phải gọi thêm API riêng để hiển thị tên. Chưa có nghiệp vụ nhập kho (Giai đoạn E) nên chưa có dữ liệu `inventory` thật — test bằng fixture tạo tạm qua Prisma (2 vật tư, 2 kho, 3 vị trí, 4 dòng tồn kho) rồi xoá sạch ngay sau khi verify xong (xem `07-DECISIONS-LOG.md`). Test thực tế: không filter → trả đủ 4 dòng; filter theo `item_id` → đúng 3 dòng; theo `warehouse_id` → đúng 3 dòng; theo `location_id` → đúng 2 dòng, đúng số liệu `quantity`/`available_quantity`; không có token → 401.
- [x] D2. `GET /api/inventory/:itemId`. Note (viết lại 2026-09-11): trả toàn bộ dòng tồn kho của 1 vật tư theo từng vị trí (không phân trang), filter thêm được `warehouse_id` qua query. Test thực tế: gọi với `item1` (nằm ở 3 vị trí thuộc 2 kho khác nhau) → trả đúng 3 dòng kèm đúng tên kho/vị trí; thêm `?warehouse_id=` → lọc còn đúng 2 dòng thuộc kho đó. Dữ liệu fixture đã dọn sạch — DB xác nhận về lại baseline (0 dòng `inventory`/`items`/`warehouses`) sau khi test.

## Giai đoạn E — Nghiệp vụ kho (mỗi bước: tạo phiếu → xác nhận có transaction → ghi stock_movements)

- [x] E1. Nhập kho: `/api/import-orders` (tạo, xem, confirm). Note (viết lại 2026-09-11): helper dùng chung `src/services/stockMovement.helper.js` — `incrementInventory(tx, {...})` (upsert inventory, cộng `quantity`+`available_quantity`), `decrementInventory(tx, {...})` (throw `ApiError(400)` ngay bên trong transaction nếu `available_quantity` không đủ — không phải kiểm tra ngoài rồi mới mở transaction, để tận dụng rollback thật của Prisma interactive transaction), `recordMovement(tx, {...})` (insert 1 dòng `stock_movements`). `import_code` tự sinh bằng `src/utils/codeGenerator.js` — `generateCode(prefix)` = `{PREFIX}-{YYYYMMDD}-{6 ký tự hex ngẫu nhiên viết hoa}` (xem `07-DECISIONS-LOG.md`). Route: `GET /`, `GET /:id`, `POST /` (tạo DRAFT), `POST /:id/confirm` — đọc = 4 role, tạo+confirm = `STAFF_WRITE_ROLES` (admin+manager+staff, theo `00-OVERVIEW.md` "Nhân viên kho: Nhập/xuất/chuyển kho"). `confirm` chặn gọi lại nếu status khác `DRAFT` (400). Test thật: tạo phiếu nhập 100 (kèm `unit_price`, `supplier_id`) → confirm → `inventory.quantity`=`available_quantity`=100 đúng; `stock_movements` ghi đúng 1 dòng `movement_type=IMPORT`, `reference_type=IMPORT_ORDER`, `quantity=100`.
- [x] E2. Xuất kho: `/api/export-orders` (tạo, xem, confirm, kiểm tra tồn kho đủ). Note (viết lại 2026-09-11): route/RBAC giống E1 (`STAFF_WRITE_ROLES` cho tạo+confirm). `confirm` set `approved_by = userId` của người gọi confirm cùng lúc với `status=CONFIRMED` trong transaction (xem `07-DECISIONS-LOG.md` — quyết định không có bước "duyệt" riêng biệt, ai xác nhận cũng là người duyệt). Test thật (script tự tạo fixture + tự dọn, xem `07-DECISIONS-LOG.md`): (1) xuất 30 từ tồn 100 → còn 70, đúng cả `quantity` và `available_quantity`; (2) xuất 1000 khi chỉ còn 70 → **400** "Tồn kho không đủ để xuất (còn 70, cần 1000)...", gọi lại `GET /api/inventory` xác nhận tồn kho **KHÔNG đổi** (vẫn 70); (3) **test rollback thật**: phiếu 2 dòng — dòng 1 (item chính, xuất 50, dư 70 nên đủ) + dòng 2 (item khác, xuất 9999, tồn kho vị trí đó = 0 nên chắc chắn thiếu) → confirm → 400, sau đó verify: dòng 1 **KHÔNG bị trừ** (vẫn 70, không phải 20), dòng 2 vẫn 0 dòng inventory (chưa từng được tạo), phiếu xuất vẫn `status=DRAFT` (không lọt thành `CONFIRMED`), và `stock_movements` chỉ có đúng 2 dòng từ 2 phiếu thành công trước đó (IMPORT + EXPORT 30) — **0 dòng rác từ phiếu thất bại/rollback** → chứng minh transaction atomic thật, không phải giả lập kiểm tra trước.
- [x] E3. Chuyển kho: `/api/transfer-orders` (tạo, xem, confirm, kiểm tra tồn kho nguồn đủ). Note (viết lại 2026-09-11): tái sử dụng nguyên `stockMovement.helper.js` — mỗi dòng gọi `decrementInventory(tx, {..., warehouse_id: from_warehouse_id, location_id: from_location_id})` TRƯỚC (throw `ApiError(400)` ngay trong transaction nếu vị trí nguồn không đủ — giống hệt cách làm ở E2, không kiểm tra ngoài transaction), rồi `incrementInventory(tx, {..., warehouse_id: to_warehouse_id, location_id: to_location_id})`, rồi ghi **2 dòng** `stock_movements` cho mỗi item: `TRANSFER_OUT` tại nguồn + `TRANSFER_IN` tại đích (cùng `reference_id`). Route/RBAC giống E1-E2 (`STAFF_WRITE_ROLES`). Cho phép chuyển giữa 2 vị trí CÙNG kho (`from_warehouse_id === to_warehouse_id`, khác `location_id`) lẫn khác kho. Test thật: (1) happy path chuyển 20 giữa 2 vị trí cùng kho → nguồn 100→80, đích 0→20 đúng, `stock_movements` ghi đúng 2 dòng (`TRANSFER_OUT` tại nguồn + `TRANSFER_IN` tại đích); (2) chuyển 9999 khi nguồn chỉ còn 80 → **400**, nguồn không đổi (vẫn 80), đích chưa từng được tạo dòng inventory (0 dòng); (3) **rollback thật**: phiếu 2 dòng — dòng 1 đủ hàng (chuyển 30/80) + dòng 2 thiếu hàng (item khác, chưa từng có tồn kho, chuyển 500) → 400, dòng 1 nguồn KHÔNG bị trừ (vẫn 80) VÀ đích KHÔNG được cộng (vẫn 20, không phải 50), phiếu vẫn `DRAFT`, `stock_movements` của phiếu này = 0 dòng.
- [x] E4. Thu hồi: `/api/recovery-orders` (tạo, xem, confirm). Note (viết lại 2026-09-11): logic giống hệt E1 (nhập kho) — dùng `incrementInventory` + ghi `stock_movements` với `movement_type=RECOVERY`, `reference_type=RECOVERY_ORDER`. Route/RBAC giống E1-E3 (`STAFF_WRITE_ROLES`). Test thật: (1) thu hồi 5 đơn vị vào vị trí đã có tồn kho (80) → tăng đúng thành 85, ghi đúng 1 dòng `stock_movements` `RECOVERY` qty=5; (2) thu hồi 7 đơn vị vào 1 vị trí CHƯA TỪNG có dòng `inventory` nào (kho khác) → `incrementInventory` tự tạo mới dòng inventory (upsert) với `quantity=7` đúng, không lỗi.
- [x] E5. Kiểm kê: `/api/stocktake-sessions` (tạo phiên snapshot, nhập actual_quantity, confirm điều chỉnh). Note (viết lại 2026-09-11): chỉ `MANAGE_ROLES` (admin+manager) cho cả tạo/nhập/confirm — khác E1-E4 (staff không tham gia kiểm kê, theo `00-OVERVIEW.md` "Quản lý kho: ...thực hiện kiểm kê"). Route nhập số lượng: `PATCH /:id/items` (body `{items:[{stocktake_item_id, actual_quantity}]}`, gọi nhiều lần được, mỗi lần tự tính lại `difference = actual_quantity - system_quantity`). Thêm hàm mới vào `stockMovement.helper.js`: `setInventoryQuantity(tx, {...})` — SET tuyệt đối `inventory.quantity = quantity` (khác hẳn `incrementInventory`/`decrementInventory` là cộng/trừ tương đối), dùng riêng cho kiểm kê. `confirm` chỉ tạo `stock_movements` + gọi `setInventoryQuantity` cho dòng có `difference !== 0` (dòng khớp hệ thống thì bỏ qua, không ghi gì). **Test thật (quan trọng nhất — chứng minh SET chứ không phải cộng nhầm):** tạo phiên kiểm kê snapshot đúng 4 dòng tồn kho hiện có của kho (`system_quantity` khớp đúng số liệu tại thời điểm tạo); confirm sớm khi chưa nhập dòng nào → 400 "Còn 4 dòng chưa nhập..."; **mô phỏng có 1 nghiệp vụ nhập kho khác xảy ra XEN GIỮA lúc snapshot và lúc confirm** (nhập thêm 20 vào dòng đã snapshot 55, tồn kho *live* lúc này là 75) rồi mới nhập `actual_quantity=50` (difference=-5, tính so với snapshot 55, không phải so với live 75) và confirm → tồn kho sau confirm ra đúng **50** (không phải 70 = 75+(-5), là kết quả sẽ SAI nếu lỡ code thành cộng `difference` vào giá trị live hiện tại thay vì SET thẳng về `actual_quantity`) — chứng minh chắc chắn đây là phép SET tuyệt đối; dòng thứ 2 difference dương (30→35, difference=+5) SET đúng thành 35; dòng thứ 3 difference=0 (20→20) → không ghi `stock_movements`, đúng tổng chỉ có **2 dòng** `ADJUSTMENT_STOCKTAKE` (qty=-5 và qty=+5, đúng dấu âm/dương); tạo phiên thứ 2 chỉ nhập 1/4 dòng rồi confirm → vẫn bị chặn 400.
- [x] E6. Thanh lý: `/api/liquidation-orders` (tạo, xem, confirm, kiểm tra tồn kho đủ). Note (viết lại 2026-09-11): logic transaction giống hệt E2 (xuất kho) — `decrementInventory` (throw `ApiError(400)` ngay trong transaction nếu thiếu hàng) + `recordMovement` (`LIQUIDATION`, `reference_type=LIQUIDATION_ORDER`) tái sử dụng `stockMovement.helper.js`. RBAC chỉ `MANAGE_ROLES` (admin+manager) cho tạo+confirm — **staff KHÔNG được** (khác E1-E4), đọc vẫn `ALL_ROLES`. `confirm` set `approved_by = userId` cùng lúc `status=CONFIRMED`. Test thật: tạo user `warehouse_staff` test qua API, login, gọi `POST /api/liquidation-orders` → **403** đúng; admin tạo+confirm thanh lý 5 (từ tồn 100) → tồn kho giảm đúng còn 95, `approver.email` trả đúng `admin@warehouse.local`; thanh lý 9999 khi chỉ còn 95 → 400, tồn kho không đổi; **rollback thật** phiếu 2 dòng (dòng 1 đủ hàng 50/95, dòng 2 thiếu hàng item khác 500/20) → 400, dòng 1 KHÔNG bị trừ (vẫn 95), phiếu vẫn `DRAFT`, `stock_movements` phiếu này = 0 dòng.
- [x] E7. `GET /api/stock-movements` (filter đầy đủ). Note (viết lại 2026-09-11): `src/services/stockMovement.service.js` (chỉ đọc, không có route ghi — bảng `stock_movements` chỉ được ghi gián tiếp qua `recordMovement()` trong 6 nghiệp vụ E1-E6). Filter: `item_id`/`warehouse_id`/`location_id`/`movement_type`/`reference_type`/khoảng ngày `from`-`to` (áp vào cột `movement_date`, dùng `gte`/`lte`, có thể truyền 1 hoặc cả 2 mốc). Sắp xếp `orderBy: movement_date asc` (tăng dần — đúng nghĩa đọc lịch sử theo trình tự xảy ra, xem `07-DECISIONS-LOG.md`). RBAC đọc = `ALL_ROLES` (4 role, kể cả report_viewer). Test thật — **sinh dữ liệu bằng chính 6 nghiệp vụ E1-E6 qua API thật, không insert tay**: 1 item chạy qua đủ chuỗi IMPORT(100) → EXPORT(20) → TRANSFER(10, locA→locB, sinh 2 dòng TRANSFER_OUT+TRANSFER_IN) → RECOVERY(5) → ADJUSTMENT_STOCKTAKE(kiểm kê, difference=-5) → LIQUIDATION(10) → lọc theo `item_id` trả đúng **7/7 loại** `movement_type`, đúng **thứ tự thời gian tăng dần** khớp trình tự thực hiện; lọc `location_id=locA` → 6 dòng (mọi loại trừ TRANSFER_IN — vì TRANSFER_IN ghi tại locB); `location_id=locB` → đúng 1 dòng (chỉ TRANSFER_IN); lọc từng `movement_type` riêng lẻ → đúng 1 dòng/loại; `reference_type=IMPORT_ORDER` → 1 dòng, `reference_type=TRANSFER_ORDER` → đúng 2 dòng (TRANSFER_OUT+TRANSFER_IN cùng 1 phiếu); lọc theo khoảng ngày bao trùm toàn bộ → 7 dòng, khoảng ngày hẹp (trước lúc chuyển kho) → đúng 2 dòng (IMPORT+EXPORT), khoảng ngày sau khi mọi thứ đã xong → 0 dòng.

## Giai đoạn F — Chuẩn bị tích hợp AI (chỉ phần Backend, chưa động vào AI Service thật)

- [x] F1. `POST /api/ai/detect` — tạm thời có thể mock (trả dữ liệu giả lập đúng cấu trúc theo `05-AI-SERVICE-SPEC.md`) vì AI Service chưa làm ở giai đoạn này. Đánh dấu rõ trong code là mock, kèm TODO khi tích hợp thật. Note (viết lại 2026-09-11): `src/services/ai.service.js` có comment MOCK + TODO chi tiết (khi có AI Service thật, thay bằng gọi HTTP `POST {AI_SERVICE_URL}/detect`, forward nguyên field `image`, trả thẳng response cùng cấu trúc — không đổi field để FE không phải sửa code). `src/routes/ai.routes.js`: Multer memory storage (field `image`, giới hạn 10MB). Quyền: `STAFF_WRITE_ROLES` (admin+manager+staff). **Cấu trúc JSON mock — hợp đồng dữ liệu cố định cho Web/Mobile, xem chi tiết đầy đủ trong `07-DECISIONS-LOG.md`.** Test thật: upload ảnh giả (fake JPEG bytes qua `multipart/form-data`) → 200, đúng cấu trúc `{detections: [{class_name,confidence,bounding_box:{x,y,width,height}}], summary: [{class_name,count}], annotated_image}` — verify từng kiểu dữ liệu (string/number) đúng; `annotated_image` đúng định dạng `data:image/jpeg;base64,...` (là ảnh gốc người dùng upload, encode lại — KHÔNG vẽ bounding box thật vì chưa có OpenCV/YOLO); không gửi file → **400** "Vui lòng chọn ảnh...".
- [x] F2. `POST /api/import-orders/from-ai` — nhận dữ liệu đã "xác nhận" (giả lập), tạo phiếu nhập như flow E1. Note (viết lại 2026-09-11): code THẬT (không mock) — `importOrder.service.js` hàm `createFromAi()` tái sử dụng `incrementInventory`/`recordMovement` từ `stockMovement.helper.js` (giống hệt logic `confirm()` của E1), nhưng gộp `create` + phần xử lý tồn kho + set `status=CONFIRMED` vào **CÙNG 1** `prisma.$transaction` duy nhất — khác flow thường (tạo DRAFT rồi mới gọi `/confirm` riêng). Lý do và chi tiết quyết định: xem `07-DECISIONS-LOG.md`. Route: `POST /api/import-orders/from-ai`, RBAC `STAFF_WRITE_ROLES` (cùng nhóm với nhập kho thường). Test thật: gọi trực tiếp với `items` mẫu (mô phỏng dữ liệu người dùng đã xác nhận trên UI sau bước AI) → **201**, phiếu tạo ra có `status: "CONFIRMED"` **ngay lập tức** (không có trạng thái `DRAFT` nào lọt ra ngoài transaction), tồn kho cộng đúng (`quantity`=`available_quantity`=7), đúng 1 dòng `stock_movements` `movement_type=IMPORT`.

## Giai đoạn G — Kiểm thử & bàn giao

- [x] G1. Test tối thiểu theo mục 6 của `02-BACKEND-SPEC.md` (transaction rollback, RBAC, tồn kho không đủ). Note: Jest + Supertest, DB test riêng `warehouse_test_db`. 6 file test (`tests/*.test.js`), **33 test case**, `npm test` pass 100%: login sai/đúng, user INACTIVE, 401/403 RBAC, nhập/xuất/chuyển/thu hồi/kiểm kê/thanh lý happy-path, tồn kho không đủ (export/transfer/liquidation) trả 400 không đổi dữ liệu, rollback transaction 2 dòng, + đợt bổ sung sau audit: DELETE cho roles (xoá thật + 409 khi đang bị tham chiếu)/item-categories (xoá thật + 409 khi có item con)/items (soft-delete)/storage-locations (soft-delete + PUT)/suppliers (soft-delete + PUT), và `POST /api/ai/detect` (thành công/thiếu file 400/RBAC 403/thiếu token 401).
- [x] G2. Postman collection đầy đủ toàn bộ API đã code. Note: `backend/postman/warehouse-backend.postman_collection.json` — 17 folder, **75 request** (bổ sung DELETE cho roles/item-categories/items/storage-locations/suppliers, PUT cho storage-locations/suppliers so với bản đầu). Test script tự động lưu id vào collection variables; các request "xoá" dùng resource tạo riêng (`*_id_to_delete`) để không phá id đang được các folder nghiệp vụ phía sau dùng lại. Sửa 1 lỗi phát hiện qua audit: "Nhap so luong thuc te" trước đây chỉ điền `actual_quantity` cho dòng đầu tiên trong phiên kiểm kê nên `Confirm stocktake session` báo 400 khi kho có ≥2 dòng tồn kho — nay dùng pre-request script điền đủ toàn bộ dòng. Đã verify bằng Newman: **75/75 request OK, 12/12 assertion pass, chạy lặp lại 3 lần liên tiếp không lỗi**.
- [x] G3. Cập nhật `07-DECISIONS-LOG.md` lần cuối cho giai đoạn này. Note: đã rà soát lại toàn bộ mục trong 07-DECISIONS-LOG.md, khớp đúng với code thực tế.

---

**Khi nào coi là xong giai đoạn Database + Backend:** tất cả checkbox trên đã tick, Postman collection chạy được toàn bộ nghiệp vụ, và `07-DECISIONS-LOG.md` phản ánh đúng những gì đã code. Lúc đó mới nên mở lại `03-WEB-SPEC.md` / `04-MOBILE-SPEC.md` / `05-AI-SERVICE-SPEC.md` cho giai đoạn tiếp theo.
