# 10 — BUILD CHECKLIST: AI SERVICE (Python + FastAPI + YOLO)

> Bối cảnh: `00-OVERVIEW.md`. Chức năng cần có: `05-AI-SERVICE-SPEC.md`. Cấu trúc JSON đã chốt làm hợp đồng dữ liệu với Backend: xem `07-DECISIONS-LOG.md` phần mock AI (Giai đoạn F Backend). Model đã huấn luyện: `ai-service/models/best.pt` (YOLOv9, fine-tune từ dataset ElectroCom61 — nguồn: https://github.com/faiyazabdullah/ElectroCom61, cần trích dẫn trong báo cáo đồ án).
>
> **Quy tắc làm việc:** giống hệt các checklist trước — làm đúng thứ tự, tick `[x]` + ghi note sau mỗi mục, ghi quyết định phát sinh vào `07-DECISIONS-LOG.md`. Đang làm trên nhánh Git `feature/ai-service` — mọi push dùng nhánh này, không push vào `main`.
>
> **Nguyên tắc quan trọng nhất của giai đoạn này:** AI Service phải trả về ĐÚNG cấu trúc JSON đã chốt (`detections`, `summary`, `annotated_image`) — Web và Mobile đã code sẵn để đọc đúng cấu trúc này từ bản mock, KHÔNG được đổi field/kiểu dữ liệu khi chuyển sang model thật, nếu không toàn bộ luồng AI ở Web/Mobile sẽ vỡ.

---

## Giai đoạn A — Huấn luyện model (đã hoàn thành ngoài checklist này)

- [x] A1. Tải dataset ElectroCom61 (2071 ảnh, 61 lớp linh kiện điện tử) từ Mendeley Data.
- [x] A2. Huấn luyện model YOLOv9 (gelan-c, 25 epochs) trên Google Colab (GPU T4 miễn phí), dùng mã nguồn fork `SkalskiP/yolov9`.
- [x] A3. Tải file trọng số `best.pt` về, đặt tại `ai-service/models/best.pt`, đã commit lên `feature/ai-service`.

## Giai đoạn B — Nền tảng AI Service

- [x] B1. Setup project Python: venv (Python 3.11.9, cài mới bằng winget vì máy chưa có Python thật). **Đổi khác checklist gốc**: KHÔNG dùng package `ultralytics` — checkpoint `best.pt` train bằng mã nguồn fork `SkalskiP/yolov9` (không phải `ultralytics` tích hợp sẵn), `ultralytics.YOLO('models/best.pt')` báo lỗi thẳng "NOT forwards compatible" (pickle checkpoint tham chiếu class `models.yolo.DetectionModel` của repo gốc, không tồn tại trong package `ultralytics`). Đã vendor mã nguồn gốc vào `ai-service/yolov9_src/` (đã bỏ `.git` lồng bên trong) và dùng thẳng code đó (`models.common.DetectMultiBackend`, `utils.*`) để load + infer. `requirements.txt` cập nhật lại theo nhu cầu thật của `yolov9_src/` (bỏ `ultralytics`, thêm `torch`/`torchvision`/`pandas`/`seaborn`/`tqdm`/`ipython`/`scipy`/`pyyaml`/`requests`/`matplotlib`/`psutil`/`setuptools<81`). Chi tiết đầy đủ + lý do: `07-DECISIONS-LOG.md`.
- [x] B2. `GET /health` — trả `{success, data: {status, model_loaded}}`. Test thật: `curl http://127.0.0.1:8001/health` → `{"success":true,"data":{"status":"ok","model_loaded":true}}`.
- [x] B3. Load model 1 lần lúc `@app.on_event("startup")` bằng `DetectMultiBackend` (`yolov9_src/models/common.py`) — không load lại mỗi request. Phải vá thêm `yolov9_src/models/experimental.py` (`attempt_load`): thêm `weights_only=False` cho `torch.load` vì PyTorch 2.6+ đổi mặc định `weights_only=True`, chặn unpickle class model tuỳ biến — an toàn vì đây là checkpoint tự huấn luyện (nguồn tin cậy).
- [x] B4. Liệt kê đủ 61 lớp qua `GET /classes` (test thật, xem danh sách đầy đủ trong `07-DECISIONS-LOG.md` — đã đối chiếu sơ bộ bằng mắt: phần lớn là tên linh kiện/module cụ thể theo chuẩn ElectroCom61, ví dụ `Resistor`, `IC-Chip`, `Arduino-Uno`, `ESP32`, nhiều loại tụ/cảm biến/module — việc đối chiếu chi tiết với danh mục `items` thật trong DB để lại cho Giai đoạn E như checklist gốc quy định).

## Giai đoạn C — Endpoint /detect (phần quan trọng nhất)

- [x] C1. `POST /detect` nhận `multipart/form-data` field `image`. Thiếu field hẳn HOẶC file rỗng đều trả 400 rõ ràng (xem D4 — có 1 bug đã sửa ở đây).
- [x] C2. Inference bằng `DetectMultiBackend` + tiền xử lý `letterbox` + hậu xử lý `non_max_suppression` + `scale_boxes` (đúng pipeline gốc của `yolov9_src/detect.py`) — lấy `class_name`, `confidence`, toạ độ box cho từng object.
- [x] C3. `detections`: mảng `{class_name, confidence, bounding_box: {x, y, width, height}}` — đúng cấu trúc đã chốt.
- [x] C4. `summary`: nhóm theo `class_name`, đếm số lượng — đã test thật với ảnh nhiều vật cùng loại lẫn khác loại (xem D1/D2), đếm đúng số lượng object model tìm thấy (KHÔNG bỏ sót object nào, dù độ chính xác phân loại của model là vấn đề riêng — xem ghi chú D1-D3).
- [x] C5. Vẽ bounding box + nhãn bằng `utils.plots.Annotator` (OpenCV bên trong), encode base64 `data:image/jpeg;base64,...` — test thật, xem ảnh minh chứng `ai-service/test_images/d1_annotated.jpg`, `d2_annotated.jpg` (box khớp chính xác từng vật thể trong ảnh gốc).
- [x] C6. JSON trả về đúng khung đã chốt `{success, data: {detections, summary, annotated_image}, message}` — đối chiếu trực tiếp với `backend/src/services/ai.service.js` (bản mock), khớp 100% field.

## Giai đoạn D — Test độc lập (chưa nối Backend)

- [x] D1. **[KẾT QUẢ VỚI MODEL CŨ — `best_v1_2071img.pt`, 2071 ảnh — ĐÃ THAY BẰNG MODEL MỚI (~13k ảnh) kể từ 2026-09-23, xem kết quả model mới ở mục "Giai đoạn D — chạy lại với model mới" bên dưới]** Ảnh 6 điện trở cùng loại (`test_images/resistors_multi.jpg`, ảnh thật tải từ Wikimedia Commons — không phải ảnh dataset train) — **kết quả: chỉ 1 detection duy nhất, độ tin cậy thấp (0.27), bị phân loại SAI thành "1-5-Volt-Battery"** (không phải "Resistor"). Đã điều tra kỹ bằng script riêng hạ ngưỡng tin cậy xuống 0.01 để xem toàn bộ ứng viên thô — lớp "Resistor" KHÔNG xuất hiện ở bất kỳ ngưỡng nào, xác nhận đây là hạn chế thật của model (model không nhận ra điện trở trong ảnh sản phẩm nền trắng studio này) chứ không phải bug pipeline — bounding box vẽ ra vẫn khớp chính xác vị trí 1 điện trở thật trong ảnh (xem `d1_annotated.jpg`), chứng minh phần code (tiền xử lý/hậu xử lý/vẽ box) hoạt động đúng, vấn đề nằm ở độ chính xác phân loại của model trên ảnh khác phong cách chụp so với dataset train (ElectroCom61).
- [x] D2. **[KẾT QUẢ VỚI MODEL CŨ — 2071 ảnh — ĐÃ THAY BẰNG MODEL MỚI, xem bên dưới]** Ảnh nhiều loại linh kiện khác nhau (`test_images/ic_multi.jpg`: IC DIP + nhiều transistor dạng TO-220, ảnh thật từ Wikimedia Commons) — **kết quả: 9 detections, box định vị chính xác từng vật thể thật** (xem `d2_annotated.jpg`) nhưng nhãn phân loại có đúng có sai: 7 gói TO-220 → "IGBT" (nhầm nhưng cùng họ gói linh kiện, hợp lý), 1 IC DIP → "Arduino-Uno" (sai), 1 transistor nhỏ → "Soil-Moisture-Sensor" (sai). `summary` nhóm/đếm đúng số lượng model tìm thấy (7+1+1=9, khớp `detections.length`).
- [x] D3. **[KẾT QUẢ VỚI MODEL CŨ — 2071 ảnh — ĐÃ THAY BẰNG MODEL MỚI, xem bên dưới]** Ảnh trắng trơn 640×480 tự tạo — không lỗi (đúng 1 phần yêu cầu), NHƯNG model báo 1 false positive "Breadboard" (conf 0.38) phủ gần hết ảnh thay vì trả rỗng như kỳ vọng — cùng loại vấn đề với D1/D2 (độ chính xác model trên input ngoài phân bố dữ liệu train), không phải lỗi service.
- [x] D4. Không gửi field `image` — **phát hiện bug thật**: FastAPI tự chặn bằng `422` (thông báo mặc định tiếng Anh) trước khi vào tới code của mình, do khai báo tham số bắt buộc `File(...)`. **Đã sửa**: đổi thành `Optional[UploadFile] = File(None)` + tự kiểm tra `None` để luôn trả `400` với thông điệp tiếng Việt nhất quán (giống Backend mock) dù thiếu hẳn field hay field rỗng. Test lại sau khi sửa: `curl -X POST .../detect` (không kèm ảnh) → `400 {"detail":"Vui long chon anh de nhan dien (field \"image\")"}`.
- [x] D5. Thời gian phản hồi thực đo trên CPU (máy dev, không có GPU): ~1.5–2.2 giây/ảnh (kích thước ảnh test ~640-960px, model YOLOv9-c 51M tham số). Ghi chú cho Giai đoạn F: Backend nên đặt timeout tối thiểu **10 giây** để có biên an toàn (ảnh thực tế từ điện thoại có thể lớn hơn/máy chạy AI Service có thể yếu hơn máy dev).

> **Lưu ý quan trọng trước khi làm Giai đoạn E**: pipeline kỹ thuật (nhận ảnh → inference → gom nhóm/đếm → vẽ box → trả JSON đúng cấu trúc) đã CHẠY ĐÚNG và được test thật đầy đủ (C1-C6, D1-D5). Vấn đề còn lại là ĐỘ CHÍNH XÁC PHÂN LOẠI của bản thân model trên ảnh chụp không giống phong cách dataset ElectroCom61 (ảnh sản phẩm studio nền trắng) — đây là giới hạn của model đã huấn luyện, cần người dùng xem xét trước khi quyết định hướng đi Giai đoạn E (có thể cần chụp ảnh test theo đúng điều kiện gần với dataset train hơn để đánh giá công bằng hơn, hoặc chấp nhận đây là hạn chế đã biết để ghi vào báo cáo đồ án).
>
> **Cập nhật 2026-09-20/23 — đã kiểm chứng bằng ảnh valid/ thật của ElectroCom61 + so sánh model cũ/mới**: test lại bằng 6 ảnh từ `valid/` (không phải `train/`, model chưa từng thấy) xác nhận model GỐC (2071 ảnh) đạt **96.3%** đúng cả vị trí lẫn tên lớp trong đúng phạm vi đã học — chứng minh vấn đề D1-D3 ở trên là DOMAIN GAP thật (ảnh Wikimedia quá khác phong cách), KHÔNG PHẢI model học chưa đủ. Sau đó người dùng train lại với ~13k ảnh (augmentation) — so sánh trực tiếp 2 checkpoint (`ai-service/compare_models.py`) cho thấy model MỚI **REGRESSED xuống 88.9%** trên cùng bộ ảnh valid/, xuất hiện lỗi nhận nhầm Resistor↔Diode với độ tin cậy cao mà model cũ không hề có. Chi tiết đầy đủ + khuyến nghị: `07-DECISIONS-LOG.md` (mục "kiểm chứng bằng ảnh valid/ thật... so sánh model cũ/mới"). **Model đang active tại `models/best.pt` hiện là bản MỚI (kém hơn theo số liệu đo được)** — người dùng cần quyết định giữ hay quay lại `models/best_v1_2071img.pt` trước khi sang Giai đoạn E.

### Giai đoạn D — chạy lại với MODEL MỚI (~13k ảnh, active tại `models/best.pt` từ 2026-09-23)

- [x] Xác nhận 61 lớp GIỐNG HỆT model cũ (cùng thứ tự, cùng tên) — test thật `GET /classes` → đúng 61 tên, so khớp danh sách với bản ghi ở B4/decisions-log, không lệch 1 ký tự nào.
- [x] D4 (test lại vì đổi model = restart service, cần xác nhận fix vẫn còn): không gửi field `image` → vẫn `400` với thông điệp tiếng Việt như cũ — fix ở tầng code, không phụ thuộc model nên không có lý do đổi, đã verify lại cho chắc.
- [x] D5 (test lại): thời gian phản hồi ~2.1–2.6 giây/ảnh trên CPU — tương đương model cũ (cùng kiến trúc YOLOv9-c 51M tham số, cùng kích thước ảnh 640px), timeout ≥10 giây ở Backend vẫn hợp lý.
- [x] D1/D2/D3 (dùng lại ĐÚNG 3 ảnh cũ để so sánh trực tiếp, xem bảng đầy đủ + bằng chứng trong `07-DECISIONS-LOG.md`):
  - `resistors_multi.jpg`: CŨ đoán sai "1-5-Volt-Battery" (0.27) → MỚI đoán sai KHÁC "Fuse" (0.52) — vẫn sai hoàn toàn, chỉ tự tin hơn.
  - `ic_multi.jpg`: CŨ tìm ra 9 vật (nhiều nhầm "IGBT") → MỚI chỉ tìm ra 2 vật (cũng "IGBT") — model mới bỏ sót nhiều hơn hẳn trên ảnh ngoài dataset.
  - `blank.jpg`: CŨ báo giả "Breadboard" (0.38) → MỚI báo giả KHÁC "Tact-Switch" (0.44) — vẫn có false positive, đổi nhãn khác.
  - **Trên bộ ảnh có nhãn thật đáng tin cậy hơn (6 ảnh `valid/` của ElectroCom61, 54 vật thể)**: model MỚI đạt 88.9% (48/54), THẤP HƠN model CŨ 96.3% (52/54), xuất hiện 3 lỗi sai lớp mới (gồm 2 lần nhầm Resistor→Diode với độ tin cậy cao 0.85-0.89) mà model cũ không có — xem chi tiết/ảnh minh chứng trong quyết định 2026-09-20/23 ở trên và bản ghi mới nhất trong `07-DECISIONS-LOG.md`.
  - **Kết luận không đổi so với lần trước**: model mới KHÔNG cải thiện, thậm chí kém hơn model cũ trên phép đo khách quan nhất hiện có. Quyết định giữ nguyên model nào là của người dùng — Giai đoạn E/F dưới đây không phụ thuộc vào việc chọn model nào (taxonomy 61 lớp và cách tích hợp giống hệt nhau), nên tiếp tục làm E/F không lãng phí công sức nếu sau này đổi lại model.

## Giai đoạn E — Mapping tên lớp AI ↔ vật tư thật (cần người dùng xác nhận)

- [x] E1. Đối chiếu 61 tên lớp model với danh mục `items` THẬT trong database (`SELECT item_code, item_name, category FROM items WHERE status='ACTIVE'` qua Prisma) — **kết quả: DB hiện chỉ có ĐÚNG 3 vật tư, cả 3 đều là dữ liệu TEST từ các giai đoạn build trước (không phải danh mục linh kiện thật)**:
  | item_code | item_name | category |
  |---|---|---|
  | TEST-IC555 | IC555 Test | Linh kien Test D |
  | TEST-RELAY | Relay Test | Linh kien Test D |
  | LOWSTOCK-TEST-1789294954 | Test Low Stock Formula | Cat With Item |

  → **0/61 lớp AI hiện có vật tư thật tương ứng trong DB** (kể cả "TEST-IC555"/"TEST-RELAY" cũng KHÔNG nên coi là khớp với lớp AI "IC-Chip"/"Relay-Module" — tên trùng ngẫu nhiên do đây vốn là item test đặt tên gợi nhớ, không phải item nhập liệu thật cho mục đích AI). Đây không phải lỗi — dự án chưa từng có bước nhập danh mục vật tư điện tử thật (nằm ngoài phạm vi checklist này). Ý nghĩa thực tế: muốn test luồng AI end-to-end có kết quả "khớp ngay" cần tạo trước vài vật tư thật với tên/mã tương ứng 1 vài lớp phổ biến (ví dụ `Resistor`, `IC-Chip`, `Arduino-Uno`) — xem bước G dưới đây đã tạo sẵn 1 vài item như vậy để test end-to-end.
- [x] E2. Xác nhận cơ chế mapping ở Web (`web/src/pages/ImportPage.tsx`) và Mobile (`mobile/lib/screens/ai_scan/ai_result_screen.dart`) — đọc code xác nhận: **cả 2 nơi đều KHÔNG so khớp chuỗi tự động** giữa `class_name` (AI trả về) và `item_name`/`item_code` — `class_name` chỉ hiển thị dạng nhãn tham khảo cạnh mỗi dòng, còn `item_id` thật sự dùng để tạo phiếu luôn do người dùng tự chọn qua dropdown liệt kê TOÀN BỘ vật tư đang có (`<Select value={row.item_id} options={items.map(...)} />` bên Web, `DropdownButtonFormField<String>` bên Mobile). Vì không có logic so khớp chuỗi nào cả, cơ chế này hoạt động đúng y hệt bất kể `class_name` đến từ mock hay từ model thật — **không cần sửa code Web/Mobile cho Giai đoạn E**, đã tự động tương thích.

## Giai đoạn F — Nối vào Backend

- [x] F1. `ai.service.js`: bỏ hoàn toàn mock, gọi HTTP thật sang AI Service bằng `fetch`/`FormData`/`Blob` có sẵn của Node 22+ (không thêm dependency — dự án đã dùng Node 26). Forward nguyên `req.file.buffer`/`req.file.mimetype` nhận từ `multer`, trả thẳng `body.data` (đã đúng cấu trúc `{detections, summary, annotated_image}`) về controller — **KHÔNG đổi field nào** so với hợp đồng mock cũ. Test thật qua HTTP đầy đủ (không đoán): `curl -F image=@... /api/ai/detect` với token thật → `200`, cấu trúc JSON đúng, `detections`/`summary` khớp kết quả model đã thấy ở Giai đoạn D (ví dụ đúng bằng chứng lỗi Resistor→Diode đã ghi nhận). `ai.controller.js` bỏ chữ "(MOCK...)" trong message thành công.
- [x] F2. Bọc `fetch` trong `try/catch`: mất kết nối (service chưa chạy/sai cổng) → `ApiError(502, 'Không kết nối được tới AI Service')`; quá thời gian (`AbortSignal.timeout(10000)`, khớp khuyến nghị D5) → `ApiError(504, ...)`; AI Service tự trả lỗi (ví dụ ảnh không hợp lệ) → forward nguyên `400` kèm thông điệp gốc, còn lỗi khác từ AI Service → `502`. Test thật: trỏ `AI_SERVICE_URL` sang cổng không tồn tại (`:9999`) → xác nhận `detect()` ném đúng `502` thay vì treo/crash tiến trình.
- [x] F3. Thêm `AI_SERVICE_URL="http://localhost:8001"` vào `.env`, `.env.example`, `.env.test` (dùng chung 1 cổng cho dev lẫn test — AI Service không có DB riêng nên không cần tách môi trường như Postgres).

> **Test suite backend cũng cập nhật theo**: `tests/ai.test.js` trước đây gửi 6 byte giả (`[0xff,0xd8,...]`) — mock cũ không kiểm tra nội dung ảnh nên vẫn qua, nhưng AI Service thật dùng `cv2.imdecode` sẽ từ chối byte rác. Đã đổi sang gửi 1 ảnh thật có sẵn trong repo (`ai-service/test_images/electrocom_valid/...jpg`). Chạy lại **toàn bộ** test suite backend (không chỉ file AI) với AI Service thật đang chạy nền: **17 test suite / 54 test — pass hết, không có regression** ở các phần khác.

## Giai đoạn G — Test end-to-end

- [x] G1. **Web** — test thật bằng trình duyệt headless (Playwright, cài tạm ngoài repo, không thêm dependency vào `web/`) điều khiển đúng luồng người dùng thật: đăng nhập (JWT tự ký bằng `jwt.sign`/`JWT_SECRET` của chính app, không đoán mật khẩu) → `/import` → "Nhập kho bằng AI" → chọn kho → tải ảnh thật → "Phân tích bằng AI" → nhận đúng kết quả thật (`AI: 5 loại • 7 vật thể`, đúng số đã biết từ Giai đoạn F) → chọn vật tư/vị trí thật cho cả 5 dòng → "Hoàn tất nhập kho" → **tạo THẬT phiếu `IMP-20260923-0B20C0`**, xác minh lại trong DB: `status: CONFIRMED`, đúng 5 dòng/7 đơn vị đã nhập vào "Kho A Test". `console --errors` trong suốt phiên: **0 lỗi**. Ảnh chụp màn hình từng bước làm bằng chứng (annotated image + bounding box đúng vị trí thật, badge tóm tắt, dropdown chọn vật tư, màn "Nhập kho thành công!").
- [x] G2. **Mobile (điện thoại thật RMX2205)** — `flutter run` gắn LAN IP thật, đăng nhập bằng tài khoản test tạo qua chính API thật (không đoán mật khẩu tài khoản có sẵn) → Nhập kho → Quét AI → **chụp ảnh THẬT bằng camera thật của máy** (không phải ảnh đẩy sẵn qua `adb push` — màn "Quét AI" chỉ hỗ trợ `ImageSource.camera`, không có lựa chọn thư viện ảnh, nên test bằng đúng luồng camera thật) chụp các linh kiện điện tử thật đang có sẵn gần đó → app tự động upload → Backend → AI Service thật → **màn "Kết quả nhận diện" hiển thị đúng ảnh đã chú thích với bounding box + nhãn thật** (`3-3-Volt-Battery 89%`, `ESP32-CAM 79%`, `Raindrops-Module 87%`, `FT-232-USB-Serial-Module`, `DC-Motor 59%`, `Taper-Potentiometer`) → xác nhận dropdown "Vật tư" liệt kê đúng 3 item thật trong DB và chọn được bình thường (test chọn "Test Low Stock Formula" cho dòng DC-Motor — thành công). `adb logcat` toàn bộ phiên (dump lại sau khi test, không chỉ theo dõi trực tiếp vì `flutter run` bị mất kết nối dev-tools giữa chừng do timeout công cụ — ứng dụng trên máy vẫn chạy bình thường, không crash) lọc `EXCEPTION CAUGHT|FlutterError|RenderFlex|BoxConstraints|Null check operator|FATAL EXCEPTION`: **0 kết quả thuộc về app** (1 dòng `FATAL EXCEPTION` duy nhất tìm thấy là log cũ từ 2 ngày trước, thuộc tiến trình hệ thống khác `MXThread0`, không liên quan).
  - **Phát hiện phụ (ghi nhận, không thuộc phạm vi AI Service)**: khi gõ email đăng ký test user qua bàn phím LabanKey Telex của máy, chữ "test" bị biến thành "tét" — TÁI HIỆN đúng bug Telex đã từng sửa cho ô tìm kiếm Tồn kho (`07-DECISIONS-LOG.md` 2026-09-15), nhưng lần này ở màn Đăng nhập (`login_screen.dart`), là màn CHƯA được sửa. Không sửa trong phạm vi commit này (ngoài checklist AI Service) — chỉ né bằng cách đặt email/mật khẩu test không chứa cụm "nguyên âm+s", và ghi lại đây để nhớ áp dụng cùng fix (`keyboardType: visiblePassword` + tắt gợi ý) cho `login_screen.dart` ở một đợt khác.
- [x] G3. So sánh với hành vi mock cũ: cấu trúc JSON (`detections`/`summary`/`annotated_image`) không đổi 1 field nào — Web/Mobile đọc và hiển thị đúng như khi còn mock, chỉ khác là dữ liệu giờ phản ánh đúng ảnh thật thay vì 7 detection cố định giả lập. Không có gì vỡ ở cả 2 nền tảng.

## Giai đoạn H — Hoàn thiện

- [ ] H1. Viết `README.md` ngắn cho `ai-service/` — cách cài đặt, cách chạy, ghi rõ nguồn dataset (trích dẫn ElectroCom61) và mã nguồn YOLOv9 fork đã dùng.
- [ ] H2. Cập nhật `07-DECISIONS-LOG.md` lần cuối cho AI Service.
- [ ] H3. Commit + push, merge vào `main` sau khi người dùng xác nhận toàn bộ ổn.

---

**Khi nào coi là xong AI Service:** toàn bộ checkbox đã tick, endpoint `/detect` trả đúng cấu trúc đã chốt, đếm số lượng đúng, test end-to-end trên cả Web lẫn Mobile với model thật không lỗi. Lúc đó dự án coi như hoàn chỉnh cả 4 phần: Database + Backend + Web + Mobile + AI Service.
