# 07 — DECISIONS LOG

> File này ghi lại các quyết định **thực tế** phát sinh trong quá trình code mà spec gốc (00–06) chưa nói rõ 100%, hoặc các chi tiết implementation mà các phần sau (Web, Mobile, AI Service) cần biết chính xác để không code lệch.
>
> **AI agent: mỗi khi tự quyết định một chi tiết không có sẵn trong spec (tên field, format response, cấu trúc lỗi, quy ước đặt tên route, enum value...), hãy thêm 1 mục mới vào đây ngay lúc đó — đừng để cuối mới nhớ lại.**

Định dạng mỗi mục:

```
## [Ngày] — [Tên quyết định ngắn gọn]
- Bối cảnh: vì sao cần quyết định
- Quyết định: chọn gì
- Ảnh hưởng tới: (Backend / Web / Mobile / AI Service — phần nào cần biết điều này)
```

---

## 2026-09-11 — Cấu trúc thư mục Backend
- Bối cảnh: spec không quy định cụ thể cấu trúc folder (B1).
- Quyết định: `src/{routes,controllers,services,middlewares,utils,validators}` chứa toàn bộ code backend; `prisma/` (schema.prisma, migrations/, seed.js) đặt ở **root** của `backend/`, KHÔNG lồng trong `src/` — vì đây là convention chuẩn của Prisma CLI (`prisma migrate`, `prisma studio`... đều tìm `prisma/schema.prisma` ở root theo mặc định, không cần cấu hình thêm). Prisma client singleton đặt tại `src/utils/prisma.js`. `tests/` và `postman/` cũng ở root `backend/`.
- Ảnh hưởng tới: Backend (mọi file code sau này phải theo đúng cấu trúc này, không tạo `src/prisma` hay đặt schema nơi khác).

## 2026-09-11 — npm install-scripts approve cho native/postinstall packages
- Bối cảnh: viết lại backend từ đầu (code cũ mất do cài lại Windows). npm bản mới trên máy (11.19.0) mặc định CHẶN install script (preinstall/install/postinstall) của package lạ trừ khi được approve tường minh — `bcrypt`, `prisma`, `@prisma/client`, `@prisma/engines`, `@parcel/watcher`, `unrs-resolver` đều cần chạy script này để build native binding / tải engine binary.
- Quyết định: chạy `npm install-scripts approve <pkg>` cho đúng 6 package trên (không dùng `--ignore-scripts` hay hạ cấp bảo mật toàn cục). Danh sách approve được lưu vào `package.json` (key `allowScripts`) nên lần install sau không cần approve lại.
- Ảnh hưởng tới: Backend — bất kỳ ai clone repo mới, chạy `npm install` trên máy có npm mới tương tự có thể thấy cảnh báo tương tự; xem `package.json.allowScripts` hoặc chạy `npm install-scripts approve` lại nếu bcrypt/Prisma lỗi native binding.

## 2026-09-11 — Docker Compose Postgres: user/db riêng thay vì mặc định `postgres`
- Bối cảnh: `06-BUILD-CHECKLIST.md` A1 chỉ yêu cầu container tên `warehouse_postgres`, không chốt sẵn user/password/db name cụ thể.
- Quyết định: user=`warehouse`, password=`warehouse`, database=`warehouse_db`, port map `5432:5432`, named volume `warehouse_postgres_data`. `DATABASE_URL` trong `.env` dùng đúng bộ này.
- Ảnh hưởng tới: Backend — ai chạy lại `docker compose up -d` trong `backend/` sẽ có đúng thông tin này; nếu cần đổi, sửa cả `docker-compose.yml` lẫn `.env`.

## 2026-09-11 — Chi tiết schema.prisma không nói rõ 100% trong 01-DATABASE-SCHEMA.md
- Bối cảnh: viết `schema.prisma` (A2), vài chi tiết field không được đánh dấu rõ nullable/required hoặc kiểu dữ liệu cụ thể trong spec.
- Quyết định:
  - `items.category_id`: **bắt buộc** (không nullable) — spec không đánh dấu "nullable" cho field này (khác với `import_orders.supplier_id` có đánh dấu rõ), nên mọi vật tư phải thuộc 1 danh mục.
  - `item_categories.parent_id`: **nullable** dù spec không ghi rõ — bắt buộc phải nullable để có danh mục gốc (không cha) trong cấu trúc cây cha-con.
  - `warehouses.manager_id`: **nullable** — thực tế kho có thể chưa gán quản lý ngay lúc tạo.
  - Các field mô tả/tự do (description, note, specifications, address, phone, contact_name, email liên hệ, website, reason, purpose, project_name, batch_number, image_url, item_type, location_name/area/shelf/drawer/box) đều **optional**; các field định danh/khóa ngoại cốt lõi và số lượng/ngày/trạng thái đều **required**.
  - `import_order_items.unit_price`: kiểu `Decimal(14, 2)` (nullable — không phải phiếu nhập nào cũng cần ghi đơn giá).
  - Tất cả PK dùng `String @id @default(uuid())` (UUID v4).
  - `storage_locations`: thêm unique composite `(warehouse_id, location_code)` — spec chỉ gợi ý đánh index `location_code`, nhưng để tránh trùng mã vị trí trong cùng 1 kho, ràng buộc unique theo cặp.
  - Enum `ReferenceType` dùng giá trị UPPER_SNAKE_CASE (`IMPORT_ORDER`, `EXPORT_ORDER`...) thay vì snake_case thường như liệt kê trong spec (`import_order`...) — theo convention enum Prisma/Postgres viết hoa.
- Ảnh hưởng tới: Backend (mọi service/controller dùng đúng các ràng buộc này), Web/Mobile (form tạo item bắt buộc chọn category; form tạo warehouse có thể bỏ trống quản lý).

## 2026-09-11 — Response helper, error mapping, JWT payload (B2-B4)
- Bối cảnh: viết middleware nền tảng (error handler, auth), cần chốt vài chi tiết implementation mà `02-BACKEND-SPEC.md` chỉ nói ở mức nguyên tắc chung.
- Quyết định:
  - Helper response dùng chung: `success(res, data, message, statusCode=200)` và `fail(res, message, statusCode=400, data=null)` trong `src/utils/response.js` — mọi controller PHẢI dùng 2 hàm này, không tự `res.json(...)` tay để tránh lệch format `{success,data,message}`.
  - Lỗi nghiệp vụ chủ động dùng `throw new ApiError(statusCode, message)` (từ `src/utils/ApiError.js`), không `return fail(...)` trực tiếp trong service — để error luôn đi qua 1 chỗ xử lý duy nhất (`errorHandler`).
  - Mapping lỗi Prisma → HTTP: `P2002` (unique constraint) → 409 "Dữ liệu đã tồn tại"; `P2003` (foreign key constraint) → 409 "Dữ liệu tham chiếu không hợp lệ hoặc đang được sử dụng"; `P2025` (record not found, ví dụ update/delete id không tồn tại) → 404. Lỗi `ZodError` → 400 kèm message ghép từ tất cả field lỗi. Lỗi khác → 500, log ra console, KHÔNG lộ message/stack thật ra response (tránh rò rỉ thông tin hệ thống).
  - JWT payload: `{ user_id, role }` (role = `role_name` dạng chuỗi, ví dụ `"admin"`, không phải `role_id`) — `authorize(...roles)` so sánh trực tiếp `req.user.role` với danh sách chuỗi role truyền vào. Secret đọc từ `process.env.JWT_SECRET`, hạn dùng từ `process.env.JWT_EXPIRES_IN` (mặc định `1d` nếu thiếu).
  - Đăng nhập với user có `status != ACTIVE` → 403 "Tài khoản đã bị khóa" (không phải 401) — phân biệt rõ với sai email/mật khẩu (401).
  - Response của `login`/`me` không bao giờ chứa `password_hash` (destructure loại bỏ trong `auth.service.js`).
- Ảnh hưởng tới: Backend (mọi route nghiệp vụ sau này dùng đúng pattern `ApiError` + `success/fail` + mapping lỗi này), Web/Mobile (biết chính xác status code/message để hiển thị lỗi, biết cấu trúc JWT không dùng được trực tiếp để lấy role_id).

## 2026-09-11 — CRUD danh mục (C1-C4): hằng số role, search filter, cập nhật mật khẩu
- Bối cảnh: viết CRUD cho 7 resource (`users`, `roles`, `item-categories`, `items`, `warehouses`, `storage-locations`, `suppliers`), cần vài quy ước chung để không lặp lại quyết định ở từng resource.
- Quyết định:
  - Gom 4 bộ role dùng chung vào `src/utils/roles.js`: `ADMIN_ONLY=['admin']`, `MANAGE_ROLES=['admin','warehouse_manager']`, `STAFF_WRITE_ROLES=['admin','warehouse_manager','warehouse_staff']`, `ALL_ROLES=` cả 4 role. Mọi route file gọi `authorize(...ROLES_CONST)` thay vì liệt kê string tay — tránh gõ sai tên role giữa các file.
  - Route `users`/`roles`: áp `authenticate + authorize(ADMIN_ONLY)` ở cấp `router.use(...)` cho TOÀN BỘ method kể cả GET (khác với các resource khác chỉ giới hạn quyền ghi).
  - Filter dạng "search" (trên `items.item_code`/`item_name`, `suppliers.supplier_name`) dùng Prisma `contains` + `mode: 'insensitive'` — tìm kiếm không phân biệt hoa/thường, khớp một phần chuỗi.
  - `PUT /api/users/:id`: field `password` là optional; nếu không gửi thì giữ nguyên `password_hash` cũ (không ép đổi mật khẩu mỗi lần sửa thông tin khác).
  - `DELETE /api/roles/:id` và `DELETE /api/item-categories/:id` là xoá thật (`prisma.x.delete`), KHÔNG tự viết code kiểm tra "còn bị tham chiếu không" — cố ý dựa vào FK constraint có sẵn trong DB (roles←users là RESTRICT mặc định vì `role_id` bắt buộc; item_categories←items cũng RESTRICT vì `category_id` bắt buộc) + `errorHandler` đã map `P2003`→409 từ B2. Đã verify thực tế cả 2 trường hợp trả đúng 409.
  - Danh sách (list) mỗi resource sắp xếp mặc định theo tên/mã tăng dần (`role_name`, `category_name`, `location_code`, `warehouse_name`, `supplier_name` ASC) hoặc `created_at` giảm dần (`users`, `items`) — chưa có yêu cầu sort tuỳ chỉnh từ FE nên chọn thứ tự dễ đọc nhất theo ngữ cảnh từng resource.
- Ảnh hưởng tới: Backend (Giai đoạn D-G tái dùng `src/utils/roles.js` thay vì định nghĩa lại), Web/Mobile (biết `search` là tìm gần đúng không phân biệt hoa thường; biết PUT user không bắt buộc gửi password).

## 2026-09-11 — Giai đoạn D: test bằng fixture tạm qua Prisma, không dùng API tạo inventory
- Bối cảnh: `GET /api/inventory`/`GET /api/inventory/:itemId` là API chỉ đọc — không có route tạo/sửa `inventory` (đúng thiết kế, vì bảng `inventory` chỉ được thay đổi gián tiếp qua các nghiệp vụ kho ở Giai đoạn E, chưa code). Cần dữ liệu thật để test filter nhưng chưa có cách hợp lệ nào tạo ra nó qua API ở giai đoạn này.
- Quyết định: viết 1 script Node dùng trực tiếp Prisma Client để tạo fixture tối thiểu (2 items, 2 warehouses, 3 storage_locations, 4 dòng inventory dàn trải để test đủ 3 kiểu filter + endpoint theo item), gọi API thật qua server đang chạy để verify, rồi TRONG CÙNG SCRIPT xoá sạch toàn bộ fixture đã tạo (thứ tự xoá đúng theo phụ thuộc FK: inventory → storage_locations → warehouses → items → item_categories) trước khi kết thúc. Không dùng Prisma Studio thủ công (khó tái lặp, dễ quên dọn) — script tự động đảm bảo dọn sạch mỗi lần chạy, kể cả khi chạy lại nhiều lần sau này để hồi quy (regression) cho Giai đoạn D.
- Ảnh hưởng tới: Backend — nếu cần test lại `GET /api/inventory` trước khi Giai đoạn E xong (nghiệp vụ nhập kho thật), lặp lại đúng cách tiếp cận này (script tạm, tự dọn), không để lại dữ liệu giả trong DB dev.

## 2026-09-11 — E1-E2: mã phiếu tự sinh, cách kiểm tra tồn kho trong transaction, RBAC nghiệp vụ kho
- Bối cảnh: viết `stockMovement.helper.js` dùng chung cho E1-E6, cần chốt vài chi tiết mà `02-BACKEND-SPEC.md` chỉ nói nguyên tắc chung ("kiểm tra đủ tồn kho", "transaction", "ghi stock_movements") chứ không nói cách implement cụ thể.
- Quyết định:
  - **Format mã phiếu** (`import_code`, `export_code`, và sẽ áp dụng tương tự cho `transfer_code`/`recovery_code`/`stocktake_code`/`liquidation_code` ở E3-E6): `{PREFIX}-{YYYYMMDD}-{6 ký tự hex viết hoa ngẫu nhiên}`, sinh bởi `src/utils/codeGenerator.js` (`generateCode(prefix)`). Prefix dùng: `IMP`/`EXP`/`TRF`/`REC`/`STK`/`LIQ`. Chọn random suffix thay vì đếm tuần tự trong ngày để tránh race condition khi 2 request tạo phiếu cùng lúc (không cần lock/transaction riêng chỉ để sinh số thứ tự).
  - **Cách kiểm tra tồn kho đủ trước khi trừ**: kiểm tra và trừ NGAY TRONG `prisma.$transaction(async (tx) => {...})`, không kiểm tra trước rồi mới mở transaction. `decrementInventory(tx, ...)` tự `throw new ApiError(400, ...)` ngay khi phát hiện `available_quantity` không đủ; Prisma interactive transaction tự động rollback toàn bộ khi callback throw — đây là cách duy nhất đảm bảo đúng nghĩa "transaction atomic thật" khi phiếu có nhiều dòng (dòng sau thiếu hàng thì dòng trước đã trừ trong cùng transaction cũng phải được hoàn tác). Đã verify thực tế: phiếu 2 dòng, dòng 1 đủ hàng dòng 2 thiếu hàng → dòng 1 KHÔNG bị trừ, phiếu vẫn `DRAFT`, không có `stock_movements` rác nào được ghi.
  - **`quantity` lưu trong `stock_movements`**: luôn là số dương (độ lớn của giao dịch); hướng tăng/giảm được thể hiện qua `movement_type` (`IMPORT` cộng, `EXPORT` trừ...), KHÔNG lưu số âm cho export — khác với `ADJUSTMENT_STOCKTAKE` (sẽ lưu `difference` có thể âm ở E5, vì đó là một con số chênh lệch chứ không phải "khối lượng giao dịch").
  - **RBAC nghiệp vụ kho — phân biệt 2 nhóm**: `import_orders`/`export_orders`/`transfer_orders`/`recovery_orders` (E1-E4) dùng `STAFF_WRITE_ROLES` (admin+manager+staff) cho CẢ tạo lẫn confirm — vì `00-OVERVIEW.md` mục 6 liệt kê rõ "Nhân viên kho: Nhập/xuất/chuyển kho, thu hồi vật tư" là việc của staff, không có bước "duyệt" riêng của quản lý kho cho 4 nghiệp vụ này. Ngược lại `stocktake_sessions`/`liquidation_orders` (E5-E6) sẽ dùng `MANAGE_ROLES` (chỉ admin+manager) vì `00-OVERVIEW.md` liệt kê "Quản lý kho: ...thực hiện kiểm kê/thanh lý" — staff không có trong danh sách này. Đọc (GET) của cả 6 nghiệp vụ đều `ALL_ROLES` (4 role, kể cả report_viewer chỉ xem).
  - **`export_orders.approved_by`**: gán bằng chính `userId` của người gọi `/confirm` (không có bước "duyệt" tách biệt với "xác nhận" — ai xác nhận cũng đồng thời là người duyệt trong transaction đó).
  - **`confirm` chặn gọi lại**: nếu `status` khác `DRAFT` (đã `CONFIRMED` hoặc `CANCELLED`) → 400, không cho confirm 2 lần (tránh cộng/trừ tồn kho trùng lặp).
- Ảnh hưởng tới: Backend (E3-E6 tái dùng nguyên `stockMovement.helper.js` + `codeGenerator.js`, áp dụng đúng nhóm RBAC theo loại nghiệp vụ), Web/Mobile (biết mã phiếu không tuần tự/không đoán được, biết `approved_by` = người bấm confirm).

## 2026-09-11 — E3-E4: chuyển kho cùng kho, thứ tự decrement/increment, upsert vị trí mới ở thu hồi
- Bối cảnh: `transfer_orders` có cả `from_warehouse_id`/`to_warehouse_id` (header) lẫn `from_location_id`/`to_location_id` (từng dòng item) — `02-BACKEND-SPEC.md` không nói rõ 2 kho ở header có bắt buộc phải khác nhau không.
- Quyết định:
  - **Cho phép `from_warehouse_id === to_warehouse_id`** (chuyển vị trí trong cùng 1 kho) — đây là trường hợp phổ biến nhất trong thực tế (đổi kệ/khay), spec `01-DATABASE-SCHEMA.md` không đặt ràng buộc 2 kho phải khác nhau, `00-OVERVIEW.md` cũng chỉ nói chung "chuyển kho". Không thêm validate chặn.
  - **Thứ tự xử lý trong transaction của `confirm`**: với mỗi dòng, LUÔN `decrementInventory` (trừ tại nguồn) TRƯỚC rồi mới `incrementInventory` (cộng tại đích) — nếu nguồn không đủ thì throw ngay, không kịp cộng vào đích (tránh tạo dòng inventory "ma" ở đích rồi phải rollback ngược). Ghi `stock_movements` (2 dòng `TRANSFER_OUT`+`TRANSFER_IN`) sau cùng, sau khi cả 2 thao tác tồn kho của dòng đó đã thành công — nếu dòng tiếp theo trong vòng lặp fail, Prisma transaction rollback cả những gì dòng trước đã làm (kể cả đã ghi `stock_movements`), nên thứ tự trong 1 dòng không quan trọng bằng việc TOÀN BỘ vòng lặp nằm trong cùng 1 `prisma.$transaction`.
  - **Thu hồi (`recovery_orders`) tái sử dụng `incrementInventory` y hệt nhập kho** — không viết logic riêng, vì bản chất nghiệp vụ giống nhau (cộng tồn kho + ghi movement), chỉ khác `movement_type`/`reference_type`. Đã verify: thu hồi vào vị trí chưa từng có dòng `inventory` → tự tạo mới (upsert) đúng, không cần phân biệt "vị trí mới" vs "vị trí đã có" ở tầng service.
- Ảnh hưởng tới: Backend (E5-E6 tiếp tục theo đúng pattern "toàn bộ vòng lặp nhiều dòng nằm trong 1 `prisma.$transaction`, throw ở bất kỳ dòng nào cũng rollback sạch"), Web/Mobile (form chuyển kho không cần chặn UI khi chọn cùng 1 kho ở nguồn/đích).

## 2026-09-11 — E5: kiểm kê snapshot toàn kho, PATCH nhập số lượng, SET tuyệt đối
- Bối cảnh: `02-BACKEND-SPEC.md` mục 4.5 mô tả nghiệp vụ kiểm kê ở mức nguyên tắc ("hệ thống snapshot", "nhân viên nhập actual_quantity", "cập nhật inventory.quantity = actual_quantity") nhưng không nói rõ API shape cho bước "tạo phiên" và bước "nhập số lượng" (2 bước tách biệt, khác hẳn E1-E4 chỉ có tạo+confirm).
- Quyết định:
  - **`POST /api/stocktake-sessions`**: chỉ nhận `{warehouse_id, stocktake_date?, note?}` — KHÔNG nhận danh sách item từ client. Server tự động snapshot TOÀN BỘ dòng `inventory` hiện có của `warehouse_id` đó tại đúng thời điểm gọi API (không cho client chọn item nào để kiểm kê — kiểm kê là kiểm toàn bộ kho, đúng tinh thần nghiệp vụ thực tế).
  - **Route nhập số lượng thực tế**: `PATCH /api/stocktake-sessions/:id/items` (không phải PUT toàn phiên), body `{items: [{stocktake_item_id, actual_quantity}]}` — cho phép gọi nhiều lần trước khi confirm (nhân viên đếm xong dòng nào nhập dòng đó, không cần nhập hết 1 lần). Mỗi lần gọi tự tính lại `difference = actual_quantity - system_quantity` (so với `system_quantity` đã snapshot cố định lúc tạo phiên, KHÔNG so với tồn kho live hiện tại).
  - **`setInventoryQuantity(tx, {...})`** (hàm mới trong `stockMovement.helper.js`): SET thẳng `inventory.quantity = quantity` tham số truyền vào, không cộng/trừ dựa trên giá trị hiện có — khác hẳn `incrementInventory`/`decrementInventory`. Đây là điểm dễ code sai nhất của cả nghiệp vụ kiểm kê (nhầm thành "cộng `difference` vào tồn kho hiện tại" thay vì "gán thẳng bằng `actual_quantity`") — 2 cách cho ra kết quả GIỐNG NHAU nếu không có nghiệp vụ nào khác xen vào giữa lúc snapshot và lúc confirm, nhưng SAI KHÁC NHAU nếu có (ví dụ có phiếu nhập kho khác xảy ra giữa lúc snapshot và confirm). Đã cố tình viết test mô phỏng đúng tình huống này để phân biệt 2 cách — xem `06-BUILD-CHECKLIST.md` E5.
  - **Dòng có `difference === 0`**: `confirm` bỏ qua hoàn toàn (không gọi `setInventoryQuantity`, không ghi `stock_movements`) — đúng nghĩa "không có biến động" thì không cần ghi log.
  - **`confirm` chặn khi còn dòng `actual_quantity === null`**: kiểm tra TRƯỚC khi mở `$transaction` (khác với kiểm tra tồn kho đủ ở E2/E3/E6 phải nằm trong transaction) — vì đây không phải race condition trên số lượng, mà là điều kiện tiên quyết về tính đầy đủ dữ liệu, không cần rollback transaction để xử lý.
- Ảnh hưởng tới: Backend, Web/Mobile (màn kiểm kê cần luồng 3 bước: tạo phiên → nhập nhiều lần qua PATCH → confirm cuối; không có form nào cho phép nhập tay `system_quantity` hay chọn item để kiểm kê).

## 2026-09-11 — E6: thanh lý giới hạn admin+manager (khác E1-E4)
- Bối cảnh: `00-OVERVIEW.md` mục 6 liệt kê "Quản lý kho: ...thực hiện kiểm kê/thanh lý" — không có "thanh lý" trong danh sách việc của "Nhân viên kho" (staff chỉ có "Nhập/xuất/chuyển kho, thu hồi vật tư").
- Quyết định: `POST /api/liquidation-orders` và `POST /:id/confirm` dùng `authorize(...MANAGE_ROLES)` (admin+manager), KHÔNG dùng `STAFF_WRITE_ROLES` như E1-E4. Đây là nghiệp vụ thứ 2 (cùng nhóm với E5 kiểm kê) bị giới hạn khỏi staff. Đã verify thực tế: token staff gọi `POST /api/liquidation-orders` → 403.
- Ảnh hưởng tới: Backend, Web/Mobile (nút "Tạo phiếu thanh lý" chỉ hiện với role admin/quản lý kho, ẩn với nhân viên kho — giống màn kiểm kê).

## 2026-09-11 — E7: thứ tự sắp xếp lịch sử biến động kho
- Bối cảnh: `02-BACKEND-SPEC.md` không nói `GET /api/stock-movements` trả về theo thứ tự tăng dần hay giảm dần theo thời gian.
- Quyết định: `orderBy: { movement_date: 'asc' }` — TĂNG DẦN (cũ nhất trước), khác với các danh sách phiếu (`import_orders`, `export_orders`...) đang sort `created_at: 'desc'` (mới nhất trước). Lý do: đây là "lịch sử biến động" của 1 vật tư/kho — đọc theo trình tự xảy ra (timeline) tự nhiên hơn khi xem lại quá trình tăng/giảm tồn kho theo mốc thời gian, khác với danh sách phiếu (nơi người dùng quan tâm phiếu MỚI TẠO trước tiên). Nếu FE muốn hiển thị mới nhất trước, tự đảo mảng ở client hoặc yêu cầu bổ sung param sort sau.
- Ảnh hưởng tới: Web/Mobile (màn lịch sử biến động kho nhận dữ liệu theo thứ tự cũ→mới, không phải mới→cũ như các danh sách phiếu khác).

## 2026-09-11 — F1: cấu trúc JSON mock CHÍNH XÁC của `POST /api/ai/detect` (hợp đồng dữ liệu cho Web/Mobile)
- Bối cảnh: AI Service (Python FastAPI + YOLO) chưa tồn tại. `05-AI-SERVICE-SPEC.md` chỉ đưa cấu trúc ví dụ/gợi ý ("cấu trúc trên là gợi ý — điều chỉnh field cho khớp"). Backend phải CHỐT CỨNG 1 cấu trúc cụ thể ngay bây giờ để Web/Mobile code UI luồng "nhập kho bằng AI" (3 bước: upload ảnh → hiển thị bounding box + bảng xác nhận → gọi `/from-ai`) mà không phải sửa lại khi AI Service thật xong.
- Quyết định — response `POST /api/ai/detect` (200, field `data` bên trong response chuẩn `{success,data,message}`):
  ```json
  {
    "success": true,
    "data": {
      "detections": [
        {
          "class_name": "resistor",
          "confidence": 0.92,
          "bounding_box": { "x": 120, "y": 80, "width": 40, "height": 25 }
        }
      ],
      "summary": [
        { "class_name": "resistor", "count": 5 },
        { "class_name": "ic_chip", "count": 2 }
      ],
      "annotated_image": "data:image/jpeg;base64,<...>"
    },
    "message": "Nhận diện thành công (MOCK — chưa tích hợp AI Service thật)"
  }
  ```
  - `detections`: mảng, MỖI phần tử là 1 vật thể nhận diện được (không gộp) — `class_name` (string), `confidence` (number, 0-1), `bounding_box` (object 4 field `x`/`y`/`width`/`height`, đều number, đơn vị pixel trên ảnh gốc).
  - `summary`: mảng đã gộp theo `class_name` — `class_name` (string), `count` (number nguyên) — Web/Mobile dùng mảng này để hiển thị bảng xác nhận số lượng theo loại, KHÔNG cần tự đếm lại từ `detections`.
  - `annotated_image`: string, luôn có tiền tố `data:image/<mime>;base64,` (data URI, không phải URL) — hiện tại ở bước mock là CHÍNH ẢNH GỐC người dùng upload (encode lại base64, không vẽ bounding box thật vì chưa có OpenCV/YOLO); khi AI Service thật tích hợp, field này vẫn giữ đúng format data URI nhưng nội dung sẽ là ảnh đã vẽ bounding box thật.
  - Request: `multipart/form-data`, field bắt buộc tên `image` (không phải `file` hay tên khác), giới hạn 10MB (Multer `limits.fileSize`).
  - Thiếu field `image` → 400, message "Vui lòng chọn ảnh để nhận diện (field "image")".
  - RBAC: `STAFF_WRITE_ROLES` (admin+manager+staff) — khớp `00-OVERVIEW.md` "Nhân viên kho: ...dùng AI hỗ trợ nhập kho".
- Ảnh hưởng tới: **Web/Mobile (QUAN TRỌNG — đây là hợp đồng dữ liệu cố định)**: form upload ảnh phải đặt tên field `image`; bảng xác nhận trước khi tạo phiếu nhập nên dựng từ `summary` (loại + số lượng), còn `detections` dùng để vẽ overlay bounding box lên `annotated_image` nếu cần hiển thị chi tiết từng vật thể; `annotated_image` render trực tiếp bằng `<img src="...">` vì đã là data URI, không cần tải thêm.

## 2026-09-11 — F2: gộp create+confirm trong 1 transaction cho luồng AI
- Bối cảnh: `02-BACKEND-SPEC.md` mục 4.1 gợi ý "có thể gộp tạo + confirm làm 1 bước" cho luồng nhập kho bằng AI nhưng không bắt buộc cách làm cụ thể.
- Quyết định: viết hàm riêng `importOrder.service.js#createFromAi()` — KHÔNG gọi lại `create()` rồi `confirm()` nối tiếp nhau (2 lệnh gọi Prisma riêng, 2 transaction riêng), mà viết logic tạo phiếu + `incrementInventory` + `recordMovement` + set `status=CONFIRMED` tất cả bên trong **1** `prisma.$transaction(async (tx) => {...})` duy nhất. Lý do: nếu tách 2 bước (dù gọi liên tiếp trong cùng request), giữa 2 bước có thể có 1 khoảng hở nơi phiếu tồn tại ở trạng thái `DRAFT` mà hệ thống (hoặc request khác) có thể đọc thấy; gộp vào 1 transaction đảm bảo bên ngoài chỉ BAO GIỜ thấy phiếu ở trạng thái `CONFIRMED` (transaction chỉ commit sau khi mọi bước xong) — đúng tinh thần "người dùng đã xác nhận trên UI rồi, hệ thống không cần thêm bước duyệt trung gian nào nữa".
- Đây là route/hàm DUY NHẤT trong toàn bộ nghiệp vụ kho làm theo kiểu "tạo xong = xác nhận luôn" — mọi nghiệp vụ khác (E1-E6) đều giữ 2 bước tách biệt (tạo DRAFT → gọi `/confirm` riêng).
- Ảnh hưởng tới: Backend (không tạo route `GET`/`PUT` sửa phiếu `from-ai` ở trạng thái DRAFT vì trạng thái đó không bao giờ tồn tại ra bên ngoài), Web/Mobile (luồng AI trên UI gọi thẳng 1 API `/from-ai` sau khi người dùng bấm "Xác nhận tạo phiếu", không cần bước "Xác nhận phiếu" riêng như các luồng nhập kho thường).

## 2026-09-11 — G1: hạ tầng test tự động (DB test riêng, reset trước mỗi lần chạy)
- Bối cảnh: cần bộ test Jest+Supertest chạy lặp lại được nhiều lần (CI, máy khác, phiên làm việc khác) mà không tích luỹ dữ liệu rác hay đụng vào DB dev đang chứa dữ liệu thật.
- Quyết định:
  - Tạo database Postgres riêng `warehouse_test_db` (cùng container `warehouse_postgres`, khác database với `warehouse_db`).
  - `backend/.env.test` (đã commit — chỉ chứa credential Docker dev cục bộ + JWT secret riêng cho test, không phải secret thật, nên an toàn để commit) trỏ `DATABASE_URL` vào `warehouse_test_db`.
  - `tests/jest.setup.js` (khai báo trong `package.json` → `jest.setupFiles`) nạp `.env.test` bằng `dotenv.config({override:true})` TRƯỚC khi bất kỳ file test nào `require('../src/app')` — vì Prisma Client singleton (`src/utils/prisma.js`) đọc `process.env.DATABASE_URL` ngay lúc `new PrismaClient()` (module load time), phải đảm bảo biến môi trường đúng đã có mặt trước đó.
  - npm lifecycle script `pretest` (`scripts/setupTestDb.js`) tự động chạy TRƯỚC `npm test`: `npx prisma migrate reset --force --skip-seed` rồi `node prisma/seed.js` trên `warehouse_test_db` — reset sạch hoàn toàn về đúng schema + seed mỗi lần, đảm bảo `npm test` là lệnh DUY NHẤT cần chạy, luôn cho kết quả nhất quán bất kể trạng thái DB trước đó.
  - **Lưu ý quan trọng cho phiên làm việc sau**: `prisma migrate reset` là thao tác phá huỷ dữ liệu — khi lệnh này được gọi bởi AI agent (Claude Code), Prisma CLI tự chặn lại và yêu cầu xác nhận tường minh từ người dùng qua biến môi trường `PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION` (đúng nguyên văn tin nhắn đồng ý). Đây là hành vi AN TOÀN, ĐÚNG NHƯ THIẾT KẾ của Prisma — không phải lỗi, không tìm cách bypass. Khi cần chạy `npm test` (hoặc bất kỳ script nào đụng `migrate reset`) trong phiên làm việc AI sau này, phải hỏi lại người dùng xác nhận, chỉ áp dụng cho `warehouse_test_db`, KHÔNG BAO GIỜ áp dụng thao tác này lên `warehouse_db`.
  - Trong lúc phát triển/debug từng file test, dùng `npx jest tests/<file>.test.js` trực tiếp (KHÔNG qua `npm test`) — vì gọi `jest` trực tiếp không kích hoạt npm lifecycle `pretest`, tránh phải xin xác nhận reset nhiều lần khi lặp code; chỉ chạy `npm test` đầy đủ (có reset) ở lần verify cuối cùng.
- Ảnh hưởng tới: Backend (mọi phiên làm việc AI sau này chạy `npm test` lần đầu sẽ cần xin xác nhận reset DB test — đây là hành vi mong đợi), người dùng (không cần tạo `warehouse_test_db` thủ công nếu đã tồn tại; nếu clone repo mới trên máy khác, cần tự `CREATE DATABASE warehouse_test_db OWNER warehouse;` một lần trước khi chạy `npm test` lần đầu).

## 2026-09-11 — G2: sinh Postman collection bằng code, lỗi IIFE scope của Newman
- Bối cảnh: collection ~112 request là quá lớn để gõ tay JSON an toàn; cần cách tái tạo/mở rộng được khi Giai đoạn Web/Mobile cần thêm endpoint.
- Quyết định:
  - Viết `backend/postman/build-collection.js` — 1 script Node thuần, định nghĩa từng request bằng object JS ngắn gọn (helper `req()`, `url()`), xuất ra `warehouse-backend.postman_collection.json` (Postman Collection v2.1 schema). Muốn cập nhật collection: sửa `build-collection.js` rồi chạy lại `node postman/build-collection.js`, KHÔNG sửa tay file JSON đã sinh ra (sẽ bị ghi đè ở lần chạy sau).
  - **Lỗi phát hiện khi chạy Newman thật lần đầu**: `SyntaxError: Identifier 'data' has already been declared`. Nguyên nhân: Newman (và Postman desktop) chạy toàn bộ test script/pre-request script của MỘT LẦN CHẠY collection trong CÙNG 1 JS context/scope (để tối ưu hiệu năng), nên `const`/`let` khai báo ở top-level trong test script của request A sẽ va chạm nếu request B (chạy sau) cũng khai báo `const`/`let` cùng tên ở top-level (ví dụ nhiều request đều đặt tên biến tạm là `data`, `items`, `types`...).
  - **Cách khắc phục**: hàm `req()` trong `build-collection.js` tự động bọc MỌI mảng `tests`/`preScript` trong 1 IIFE (`(function () { ...code... })();`) trước khi ghi vào `event[].script.exec`. Điều này tạo scope hàm riêng cho từng request, loại bỏ hoàn toàn khả năng đụng độ tên biến dù đặt tên trùng ở nhiều request khác nhau.
  - Ảnh hưởng ảnh upload cho `POST /api/ai/detect`: dùng file thật `backend/postman/fixtures/fake-image.jpg` (JPEG hợp lệ tối thiểu, do mock không đọc nội dung ảnh) tham chiếu bằng đường dẫn tương đối `fixtures/fake-image.jpg` trong `formdata` — khi chạy Newman BẮT BUỘC phải truyền `--working-dir postman` (hoặc chạy Newman từ trong thư mục `postman/`) để Postman/Newman resolve đúng file, nếu không request sẽ lỗi "file not found".
  - Cài `newman` làm devDependency chỉ để verify collection cục bộ (không phải phụ thuộc runtime của backend); `npm audit` báo một số lỗ hổng ở dependency cũ của `newman` (qua `request`/`har-validator`...) — chấp nhận được vì đây là công cụ dev-only, không bao giờ chạy trong sản phẩm thật, không ảnh hưởng bề mặt tấn công của API.
- Ảnh hưởng tới: Backend (bất kỳ ai sửa/thêm request vào collection qua `build-collection.js` PHẢI giữ nguyên cơ chế bọc IIFE nếu tự viết thêm hàm build request khác), Web/Mobile (không liên quan trực tiếp, nhưng collection này là tài liệu tham chiếu chính xác nhất cho format request/response thật của từng API).

## 2026-09-12 — Rà soát lại số lượng "17 màn hình Web" trước khi bắt đầu tích hợp
- Bối cảnh: trước khi sửa code Figma Make trong `web/`, cần xác nhận đã đủ 17 màn hình theo thiết kế gốc hay còn thiếu.
- Xác nhận: nguồn liệt kê đúng 17 màn hình là `web/src/imports/pasted_text/app-design-spec.md` (dòng 20-38 — đây chính là prompt gốc dùng để sinh code Figma Make, không phải `03-WEB-SPEC.md` mục 2 vốn chỉ liệt kê 14 dòng vì gộp chung 2 cặp CRUD liên quan — "Danh mục vật tư"+"Vật tư", "Kho"+"Vị trí lưu trữ" — thành 1 dòng mỗi cặp). Đối chiếu 17 màn hình đó với 15 file thực tế trong `web/src/pages/`: khớp đủ 100%, KHÔNG thiếu trang nào — 2 file (`CategoriesPage.tsx`, `WarehousesPage.tsx`) mỗi file dùng 2 tab nội bộ (`useState<'categories'|'items'>`, `useState<'warehouses'|'locations'>`) để phủ 2 màn hình/file, 13 file còn lại là 1-màn-hình-1-file.
- Quyết định: giữ nguyên cấu trúc 2 trang dùng tab nội bộ này (không tách thành 4 file riêng) trong giai đoạn tích hợp API — chỉ tách thành route/file riêng sau này NẾU quyết định đổi sang React Router (mục C, xem quyết định điều hướng riêng khi tới Giai đoạn C) và thấy URL sâu (`/danh-muc`, `/vat-tu`...) thực sự cần thiết cho từng tab.
- Ảnh hưởng tới: Web (không có việc "tạo trang mới" nào phát sinh từ việc rà soát này — toàn bộ 17 màn hình đã có sẵn code Figma Make, công việc còn lại là thay mock bằng API thật + áp token màu + xử lý quyền, không phải viết trang mới).

## 2026-09-12 — Giai đoạn A: hạ tầng dự án Web
- Bối cảnh: `web/` (code Figma Make copy nguyên) chỉ có `react`+`react-dom`+Tailwind v4, chưa có router/data-fetching/form lib/API client nào — cần dựng hạ tầng trước khi đụng vào từng trang.
- Quyết định A1 (cài đặt): dùng `pnpm` (đúng `web/.mise.toml` + `pnpm-lock.yaml` có sẵn) qua `npx pnpm@10.34.3` vì môi trường AI agent không có sẵn `pnpm`/`corepack` toàn cục. Cài thêm: `react-router-dom` (routing), `axios` (API client), `@tanstack/react-query` (cache/refetch state server), `react-hook-form` + `zod` + `@hookform/resolvers` (form + validate). Không cài SWR (chọn 1 trong 2 theo gợi ý A1, react-query phổ biến hơn cho REST CRUD nhiều entity như dự án này).
- Quyết định A2 (Design Tokens): tạo `web/src/styles/tokens.css`, import vào `src/index.css` (`@import './styles/tokens.css';` ngay sau `@import 'tailwindcss';`). Dùng cú pháp `@theme { --color-xxx: #hex; }` của Tailwind v4 (CSS-first config, khớp `AGENTS.md` — dự án này KHÔNG dùng `tailwind.config.js`) — mỗi biến `--color-xxx` tự sinh utility `bg-xxx`/`text-xxx`/`border-xxx`/`from-xxx`/`to-xxx`. Giá trị chốt (khớp 100% `app-design-spec.md`):
  - `--color-brand-from: #4F46E5` / `--color-brand-to: #7C3AED` — gradient thương hiệu (sidebar, nút chính, hero, panel login). Dùng: `bg-gradient-to-br from-brand-from to-brand-to` thay vì `from-indigo-500`/`from-indigo-600` hoặc `style={{background:'linear-gradient(...)'}}` hardcode.
  - `--color-danger: #DC2626` (cảnh báo/thiếu hàng), `--color-warning: #F59E0B` (sắp hết/chờ xử lý), `--color-success: #16A34A` (ổn định/thành công) — dùng cho các chỗ hiện đang so sánh rồi gán hex tay (ví dụ thanh % sử dụng vị trí kho trong `WarehousesPage.tsx`).
  - `--color-info: #2563EB`, `--color-accent: #9333EA` — khớp 2 màu còn lại trong bộ 7 màu Badge (`blue`, `purple`) đang dùng trong `components/PageLayout.tsx`, để nếu cần dùng raw (ngoài class Badge có sẵn) thì có token thay vì tự chế hex mới.
  - `--color-bg: #F8F9FB` (nền chính), `--color-surface: #FFFFFF` (nền card), `--color-border: #E5E7EB` (viền).
  - Đã kiểm chứng cơ chế hoạt động thật (build thử với class `bg-brand-from` tạm thời trong `index.html`, xác nhận compile ra đúng `--color-brand-from:#4f46e5` trong CSS output, sau đó revert) — Tailwind v4 chỉ sinh custom property cho token nào THỰC SỰ được 1 class nào đó tham chiếu (tree-shake theo nội dung quét được), nên token chưa dùng ở đâu sẽ không tự xuất hiện trong CSS build cho tới khi trang nào đó dùng — đây là hành vi đúng của Tailwind v4, không phải lỗi.
  - **Không** migrate toàn bộ 15 trang sang dùng token ngay trong Giai đoạn A — việc thay hex/class rải rác bằng token sẽ làm dần khi sửa từng trang ở Giai đoạn C-G (vì mỗi trang đang cần sửa để nối API thật rồi), rà soát lại toàn bộ lần cuối ở H4 theo đúng `08-WEB-BUILD-CHECKLIST.md`.
- Quyết định A3 (API client): tạo `web/src/services/apiClient.ts` — instance `axios` riêng, `baseURL` đọc `import.meta.env.VITE_API_URL` (fallback `http://localhost:5000/api`, khớp `backend/.env` PORT=5000). Request interceptor tự gắn header `Authorization: Bearer <token>` (đọc qua `getToken()` — hiện lưu ở `localStorage` key `wms.token`). Response interceptor: nếu status 401 → tự xoá token + gọi `unauthorizedHandler` (callback do `AuthContext` ở Giai đoạn C đăng ký qua `setUnauthorizedHandler()`, tách rời khỏi `apiClient.ts` để file này không phải import React Router trực tiếp); mọi lỗi khác ném `ApiError` kèm `message` lấy từ đúng field `message` trong envelope `{success,data,message}` của Backend thật. Hàm helper `request<T>()` gọi `apiClient` rồi unwrap thẳng `response.data.data` — các file `*.service.ts` viết ở Giai đoạn C trở đi dùng hàm này, không tự gọi `axios` trực tiếp.
- Quyết định A4 (cấu trúc thư mục): theo đúng gợi ý checklist `src/{components,pages,layouts,hooks,services,utils,contexts}` — nhưng CHỈ tạo thư mục khi có file thật cần đặt vào (đã tạo `services/` cho `apiClient.ts`; `contexts/` sẽ tạo ở Giai đoạn C khi viết `AuthContext`; `hooks/` khi viết `usePermission()` ở C4; `layouts/` nếu quyết định tách `Sidebar.tsx`/`PageLayout.tsx` ra khỏi `components/` — chưa quyết định, để ở Giai đoạn C). Không tạo thư mục rỗng trước.
- Tạo `web/.env.example` (force-add vào git dù `.gitignore` có `.env*` — theo đúng tiền lệ `backend/.env.example`/`backend/.env.test` đã làm) chứa `VITE_API_URL="http://localhost:5000/api"`; `web/.env` (không commit) copy từ file này để dev cục bộ.
- Đã verify: `pnpm run build` (Vite) và `npx tsc --noEmit` đều pass sau khi cài toàn bộ dependency mới + thêm `tokens.css` + `apiClient.ts` + cập nhật `vite-env.d.ts` (khai báo type `ImportMetaEnv.VITE_API_URL`).
- Ảnh hưởng tới: Web (mọi service API viết từ Giai đoạn C trở đi PHẢI qua `services/apiClient.ts#request()`, không tự tạo instance axios khác; mọi màu mới cần dùng PHẢI thêm vào `tokens.css` chứ không hardcode hex/class Tailwind mặc định tại chỗ).

## 2026-09-12 — Giai đoạn B: Component Library — audit + chuẩn hoá
- Bối cảnh: rà soát toàn bộ component đã có trong code Figma Make (`components/PageLayout.tsx`) trước khi sửa từng trang ở Giai đoạn C trở đi, để không phải sửa lặp lại nhiều lần.
- **Kết quả audit** (giữ nguyên / sửa / viết mới):
  - Giữ nguyên (đã đúng chuẩn): `Card` (content card), `Th`/`Td`.
  - Sửa (thiếu trạng thái): `Btn` — thêm `disabled` (chưa từng có, mọi nút đều luôn bấm được kể cả khi logic cần khoá); `Input`/`Select` — thêm `error` (viền+text đỏ) và `disabled`.
  - Viết mới hoàn toàn trong `components/ui.tsx` (chưa tồn tại ở đâu): `Checkbox`, `Radio`, `SearchInput`, `Toast`+`ToastProvider`+`useToast` (4 loại success/error/warning/info, mount 1 lần ở `App.tsx` bọc toàn app kể cả màn Login), `Breadcrumb`, `EmptyState`, `TableSkeleton`.
  - Viết mới để thay thế code lặp tay nhiều nơi: `Modal` (trước đó viết tay giống hệt nhau ở `ExportPage`/`ImportPage`/`UsersPage`), `Tabs` (viết tay giống hệt ở `CategoriesPage`/`WarehousesPage`), `Avatar` (3 kiểu tự chế khác nhau ở `Sidebar`/`UsersPage`/`RolesPage` — `RolesPage` còn tự chế 1 ternary màu riêng trùng lặp logic `Badge`), `Pagination` (viết tay giống hệt ở `InventoryPage`/`HistoryPage`), `Stepper` (viết tay riêng trong `ImportPage`), `StatCard` (2 kiểu khác nhau ở `DashboardPage` hero và `InventoryPage`/`UsersPage` summary — `StatCard` hỗ trợ `variant="hero"|"flat"`).
  - Đã áp dụng lại các component mới vào đúng những nơi bị lặp code nêu trên (không chỉ tạo component rồi để đó): `Sidebar.tsx`, `CategoriesPage.tsx`, `WarehousesPage.tsx`, `InventoryPage.tsx`, `HistoryPage.tsx`, `ImportPage.tsx`, `ExportPage.tsx`, `UsersPage.tsx`, `RolesPage.tsx`, `DashboardPage.tsx`. Nhân tiện thay luôn phần lớn hex cứng còn sót trong các file này bằng token `var(--color-...)`/class Tailwind từ `tokens.css` (Giai đoạn A2) — còn sót lại có chủ đích (chưa đụng tới vì B không có lý do component nào chạm vào các file này): `LoginPage.tsx`, `SuppliersPage.tsx` — sẽ dọn nốt khi Giai đoạn C (Auth) và Giai đoạn E chạm tới 2 trang này, hoặc chốt tại H4.
- **Phát hiện quan trọng về Badge/trạng thái phiếu** (kiểm tra riêng theo yêu cầu): Backend thật (`backend/prisma/schema.prisma:17-21`, enum `OrderStatus`) chỉ có đúng 3 giá trị **DRAFT / CONFIRMED / CANCELLED** — không có PENDING/APPROVED/REJECTED. Mock Figma Make lại tự bịa mỗi trang nghiệp vụ (Import/Export/Transfer/Recovery/Stocktake/Disposal) một "state machine" khác hình dạng: có trang 2 trạng thái, có trang 4 trạng thái, tên field cũng khác nhau (`pending`/`approved`/`done`/`completed`). Đây là dữ liệu mock sẽ bị thay THẲNG bằng response thật (đúng 3 trạng thái) khi nối API ở Giai đoạn F — không sửa state machine ở Giai đoạn B vì đó là dữ liệu/luồng nghiệp vụ, không phải component.
  - Trong lúc rà soát, phát hiện VÀ sửa 1 lỗi thật (không phụ thuộc việc trên): nhãn **"Hoàn tất"** dùng màu xanh lá (`green`) ở `TransferPage`/`RecoveryPage` nhưng lại xám (`gray`) ở `DisposalPage` — cùng 1 nhãn, 2 màu khác nhau. Đã sửa `DisposalPage.tsx` về `green` cho khớp. Component `Badge` tự nó không có lỗi (7 màu, style nhất quán `bg-{color}-50 + border`) — lỗi nằm ở nơi GỌI Badge (mock `statusConfig` mỗi trang tự khai báo), không phải bản thân component.
  - `HistoryPage.tsx`'s `typeConfig` (màu theo LOẠI nghiệp vụ: nhập=xanh lá, xuất=đỏ, chuyển=xanh dương, thu hồi=tím, kiểm kê=vàng, thanh lý=xám) giữ nguyên — đây là 1 trục ý nghĩa KHÁC (loại nghiệp vụ, không phải trạng thái phiếu), đúng theo `app-design-spec.md:36`, không phải lỗi.
- **Bug thật phát hiện qua kiểm thử trình duyệt thật (Playwright, không phải chỉ build/tsc)**: `ImportPage.tsx` khai báo `useState` cho `detailId` SAU 1 câu lệnh `return` có điều kiện (`if (view === 'create') return (...)`) — vi phạm Rules of Hooks. Khi vào màn "Tạo phiếu nhập" (số lượng hook giảm từ N+1 xuống N giữa 2 lần render), React ném lỗi thật `"Rendered fewer hooks than expected"` ngay trên trình duyệt (xác nhận bằng `page.on('pageerror')`). Đây là lỗi CÓ SẴN trong code Figma Make gốc (không phải do sửa ở B gây ra), chỉ lộ ra khi thao tác thật trên trình duyệt — build/tsc không bắt được vì đây là lỗi runtime theo thứ tự gọi hook, không phải lỗi kiểu dữ liệu. Đã sửa: chuyển khai báo `useState(detailId)` lên đầu component, trước mọi `return`. Đã verify lại bằng Playwright: vào được màn tạo phiếu + stepper 3 bước hiển thị đúng, không còn lỗi console.
- **Đã verify bằng trình duyệt thật** (Playwright headless, `npx playwright install chromium` + script điều khiển `web/` dev server tại `localhost:8443`, không chỉ dựa vào `tsc`/`vite build`): Dashboard (StatCard hero + gradient token), Users (Modal thêm người dùng + Avatar theo màu vai trò), Roles (Avatar sửa đúng bug ternary cũ), Categories (Tabs chuyển đúng giữa 2 tab), Warehouses (Tabs chuyển đúng), Import (Stepper 3 bước hiển thị đúng sau khi sửa bug hook) — không còn lỗi console, không có regression hình ảnh so với bản gốc.
- Ảnh hưởng tới: Web (từ Giai đoạn C trở đi, mọi trang cần Modal/Tabs/Avatar/Pagination/Toast/Stepper/StatCard/Checkbox/Radio/SearchInput/EmptyState/Breadcrumb/TableSkeleton PHẢI import từ `components/ui.tsx`, không viết tay lại; khi nối API thật cho 6 trang nghiệp vụ ở Giai đoạn F, thay `statusConfig` mock bằng đúng 3 giá trị DRAFT/CONFIRMED/CANCELLED thống nhất 1 map dùng chung thay vì mỗi trang tự khai báo).

## 2026-09-12 — Giai đoạn C: App Shell & Auth & Phân quyền
- **Quyết định điều hướng (C, mục #4 việc ban đầu)**: chọn **React Router** (`react-router-dom`, đã cài ở Giai đoạn A1), KHÔNG giữ callback `onNavigate` đơn giản của Figma Make. Lý do: yêu cầu C5 "route guard chặn truy cập trực tiếp URL" chỉ có ý nghĩa khi tồn tại URL thật cho từng trang — với cách cũ (1 URL `/` duy nhất, đổi `currentPage` bằng React state) không có khái niệm "gõ thẳng URL" để test/chặn. Áp dụng URL đúng bằng tên `Page` hiện có (`/dashboard`, `/inventory`, `/categories`, ... `/roles`) cho TOÀN BỘ 14 route, không trộn lẫn 2 cách. `App.tsx` viết lại hoàn toàn bằng `<BrowserRouter><Routes>`; 2 trang duy nhất có prop `onNavigate` cũ (`DashboardPage`, `InventoryPage`) đổi sang gọi `useNavigate()` nội bộ (giữ nguyên tên biến `onNavigate` bên trong hàm để diff tối thiểu, chỉ đổi implementation).
- **`UserRole` đổi sang khớp CHÍNH XÁC `role_name` thật của Backend**: `'admin' | 'warehouse_manager' | 'warehouse_staff' | 'report_viewer'` (trước đó Figma Make tự đặt alias rút gọn `'admin'|'manager'|'staff'|'viewer'`). Lý do: role giờ lấy thật từ JWT payload (`backend/src/services/auth.service.js:32` ký `role: user.role.role_name`) và `GET /api/auth/me`, không còn là state demo tự đặt tên tuỳ ý — dùng thẳng string thật của Backend để không cần lớp map hai chiều dễ lệch. `ROLE_LABELS` (`types.ts`) cập nhật theo key mới. Trang `UsersPage`/`RolesPage` (mock nội bộ, thuộc phạm vi Giai đoạn G) KHÔNG bị đổi vì mảng dữ liệu mẫu của chúng dùng string cục bộ độc lập, không import type `UserRole`.
- **C1 (App Shell)**: giữ nguyên layout sidebar+content đã có (đúng thiết kế, đã verify ảnh ở Giai đoạn B) — tách phần bọc `<div className="flex h-full">...</div>` từ `App.tsx` ra `layouts/AppShell.tsx` (route cha dùng `<Outlet/>` cho các route con), theo đúng quy ước thư mục đã ghi ở Giai đoạn A4.
- **C2 (Login thật)**: `LoginPage.tsx` viết lại — gọi `useAuth().login(email,password)` (gọi thật `POST /api/auth/login` qua `auth.service.ts`), lỗi hiển thị bằng banner đỏ (đọc `ApiError.message` — đúng field `message` Backend trả, ví dụ "Email hoặc mật khẩu không đúng"/"Tài khoản đã bị khóa"). Đăng nhập xong điều hướng về trang đã định vào trước đó nếu có (`location.state.from`, ví dụ bị bounce từ 1 URL cần login), mặc định về `/dashboard`. **Đã xoá** 2 phần tử giao diện giả không có chức năng thật đứng sau (checkbox "Ghi nhớ đăng nhập", nút "Quên mật khẩu?") — cả hai đều không có API tương ứng trong `02-BACKEND-SPEC.md`/`03-WEB-SPEC.md`, để lại sẽ là UI hứa hẹn chức năng không tồn tại (cùng loại vấn đề với "Vai trò demo").
- **C3 (AuthContext)**: `contexts/AuthContext.tsx` — mount 1 lần ở gốc `App.tsx`. Lúc khởi tạo: nếu có token cũ trong `localStorage` (từ lần trước, `apiClient.ts` Giai đoạn A3), gọi `GET /api/auth/me` để "hồi phục" phiên đăng nhập (test: F5 reload trang vẫn giữ đăng nhập — xem bằng chứng bên dưới); nếu token hỏng/hết hạn (401) thì xoá token, coi như chưa đăng nhập. `apiClient.ts#setUnauthorizedHandler()` (đã chuẩn bị sẵn ở Giai đoạn A3, chưa dùng tới) nay được `AuthProvider` đăng ký — MỌI lời gọi API (không riêng `/me`) trả 401 giữa lúc dùng app sẽ tự set `user=null`, kết hợp route guard bên dưới tự bounce về `/login`.
- **C4 (phân quyền UI dùng chung)**: `config/permissions.ts` — 1 object `PAGE_ACCESS: Record<Page, UserRole[]>` DUY NHẤT (khớp bảng `00-OVERVIEW.md` mục 6), thay hoàn toàn hàm `canSee()` if/else cũ từng nằm trong `Sidebar.tsx`. Hook `hooks/usePermission.ts` đọc role từ `useAuth()`, expose `canAccess(page)`. `Sidebar.tsx` viết lại: dùng `usePermission()` để ẩn/hiện menu, dùng `<NavLink to={...}>` thật (không phải `onClick` giả) để có URL thật + trạng thái active tự động theo route hiện tại. **Đã xoá HOÀN TOÀN khối "VAI TRÒ DEMO"** (dropdown `<select>` đổi role tay) — tên/avatar/vai trò hiển thị ở chân sidebar giờ lấy thật từ `useAuth().user` (Backend trả `full_name`, `role.role_name`), không còn prop `userName`/`userRole`/`onRoleChange` giả nào trong `Sidebar.tsx`.
- **C5 (Route guard)**: `components/RouteGuards.tsx` — `RequireAuth` (chưa đăng nhập → `<Navigate to="/login">`, có lưu `state.from` để quay lại đúng trang sau khi login), `RequireGuest` (đã đăng nhập mà cố vào `/login` → bounce `/dashboard`), `PagePermission` (bọc từng `<Route>` theo đúng `page` — nếu role không đủ quyền, render thẳng `<Forbidden/>` ở TẦNG ROUTE, không phải chỉ ẩn menu). Áp dụng cho ĐỦ 14 route trong `App.tsx`.
- **Tài khoản test 4 vai trò**: seed gốc (`backend/prisma/seed.js`) chỉ có 1 admin. Đã tạo 3 tài khoản còn lại **qua chính API thật** (`POST /api/users` bằng token admin, không sửa tay DB) để test đúng luồng thật: `manager.test@warehouse.local` (Manager@123, warehouse_manager), `staff.test@warehouse.local` (Staff@123, warehouse_staff), `viewer.test@warehouse.local` (Viewer@123, report_viewer). 3 tài khoản này tồn tại thật trong `warehouse_db` (không phải test DB) — giữ lại để dùng cho H1 (test thủ công 4 vai trò) sau này.
- **Đã verify bằng Playwright thật** (dựng cả Backend thật `localhost:5000` lẫn Web dev server `localhost:8443`, không chỉ tin build/tsc pass — tiếp tục cách làm đã dùng ở Giai đoạn B):
  1. Đăng nhập lần lượt cả 4 tài khoản → chụp ảnh sidebar từng vai trò, xác nhận đúng khớp `00-OVERVIEW.md`: Admin (đủ 14 mục), Quản lý kho (đủ trừ Người dùng/Vai trò), Nhân viên kho (chỉ Dashboard/Tồn kho/Kho & vị trí/4 nghiệp vụ, không Kiểm kê/Thanh lý/Danh mục/NCC/Lịch sử), Người xem báo cáo (chỉ Dashboard/Tồn kho/Lịch sử biến động). Tên+vai trò hiển thị đúng dữ liệu thật (`Administrator`/`Test Manager`/`Test Staff`/`Test Viewer`), không còn "Cao Xuân Khu" cứng hay dropdown demo.
  2. Gõ thẳng URL trang không có quyền: `staff` → `/users`, `viewer` → `/import` — cả 2 đều hiện đúng màn "Không có quyền truy cập" ở vùng nội dung (sidebar vẫn theo đúng vai trò, KHÔNG phải chỉ ẩn menu mà route vẫn lọt dữ liệu — dữ liệu thật KHÔNG render).
  3. JWT hỏng (set `localStorage['wms.token']` thành chuỗi rác) rồi vào thẳng `/dashboard` → tự động bounce về `/login` (do `GET /me` trả 401 → `unauthorizedHandler` → `RequireAuth` redirect).
  4. Bonus (không có trong yêu cầu nhưng liên quan trực tiếp C3/C5, verify luôn cho chắc): F5 reload khi đang đăng nhập → giữ nguyên phiên (không bị đá về login); vào thẳng `/login` khi đã đăng nhập → tự bounce `/dashboard`; bấm "Đăng xuất" → về `/login`, sau đó gõ lại `/dashboard` → bị chặn về `/login` (không còn phiên).
  - Không có lỗi console (`page.on('pageerror')` rỗng) ở toàn bộ kịch bản trên.
- Ảnh hưởng tới: Web (từ Giai đoạn D trở đi, MỌI trang mới phải thêm route + `PagePermission` tương ứng trong `App.tsx`, cập nhật `PAGE_ACCESS` trong `config/permissions.ts` nếu có trang mới; không tự thêm `if (role === ...)` rải rác ở component khác — luôn qua `usePermission()`); người kiểm thử thủ công H1 có thể dùng ngay 4 tài khoản test đã tạo ở trên thay vì phải tự tạo lại.

## 2026-09-12 — Bổ sung C4: phân quyền GHI ở cấp resource (không chỉ cấp trang)
- Bối cảnh: người dùng phát hiện `PAGE_ACCESS` (chốt lúc đầu Giai đoạn C) chỉ chặn ở cấp TRANG — không đủ cho `WarehousesPage` (gộp 2 tab "Kho" + "Vị trí lưu trữ" bằng tab nội bộ, quyết định ở Giai đoạn B) vì 2 resource này có quyền GHI khác nhau thật sự ở Backend.
- Đối chiếu lại TOÀN BỘ `backend/src/routes/*.js` (không đoán, đọc trực tiếp từng `authorize(...)`) để lập bảng quyền ghi chính xác theo `backend/src/utils/roles.js` (`ADMIN_ONLY`/`MANAGE_ROLES`/`STAFF_WRITE_ROLES`/`ALL_ROLES`):
  - `ADMIN_ONLY`: `users`, `roles`.
  - `MANAGE_ROLES` (admin+manager): `item_categories`, `items`, `warehouses`, `suppliers`, `stocktake_sessions`, `liquidation_orders`.
  - `STAFF_WRITE_ROLES` (admin+manager+staff): `storage_locations`, `import_orders`, `export_orders`, `transfer_orders`, `recovery_orders`, `ai_detect`.
  - Đọc (GET) mọi resource đều `ALL_ROLES` (trừ users/roles là `ADMIN_ONLY` luôn cả đọc).
  - Kết luận: `CategoriesPage` (item_categories + items) — CÙNG quyền ghi (`MANAGE_ROLES` cả 2 tab), không có gap. `WarehousesPage` (warehouses + storage_locations) — KHÁC quyền ghi thật (`MANAGE_ROLES` vs `STAFF_WRITE_ROLES`) — đây LÀ gap có thật, đã sửa.
- Quyết định: thêm `RESOURCE_WRITE_ACCESS: Record<WritableResource, UserRole[]>` trong `config/permissions.ts` — mịn hơn `PAGE_ACCESS`, khớp 1:1 với từng resource thật (đủ 14 resource, không chỉ warehouses/storage_locations). `usePermission()` thêm `canWrite(resource)`. Đây vẫn là "1 nguồn duy nhất" đúng tinh thần C4 — chỉ là có 2 tầng (trang + resource) thay vì 1 tầng, không phải rải `if (role === ...)` ở nơi khác.
- Áp dụng: `WarehousesPage.tsx` — nút "Thêm kho"/"Thêm vị trí" (label đổi theo tab) giờ ẩn hẳn nếu `!canWrite('warehouses' | 'storage_locations')` tương ứng với tab đang mở. `CategoriesPage.tsx` — áp dụng tương tự cho tính nhất quán (`canWrite('item_categories' | 'items')`), dù hiện tại 2 giá trị giống nhau nên KHÔNG đổi hành vi hiển thị — mục đích là tránh giả định "cả trang 1 quyền" quay lại nếu Backend đổi RBAC sau này (ví dụ nếu sau này cho staff sửa `items` nhưng không sửa `item_categories`).
- **Đã verify bằng Playwright thật**: đăng nhập `staff.test@warehouse.local`, vào `/warehouses` → tab "Danh sách kho": 0 nút "Thêm kho" (đúng, staff không có quyền); chuyển tab "Vị trí lưu trữ": có đúng 1 nút "Thêm vị trí" (đúng, staff có quyền). Không lỗi console.
- **Phát hiện thêm (CHƯA sửa, cần xác nhận trước khi đổi vì thay đổi hành vi hiển thị cho role viewer)**: `00-OVERVIEW.md` mục 6 ghi quyền "Người xem báo cáo": "...danh sách vật tư/thiết bị (read-only)" — nhưng `PAGE_ACCESS.categories` hiện KHÔNG có `report_viewer` (kế thừa nguyên trạng từ hàm `canSee()` gốc của Figma Make, vốn cũng đã thiếu). Có 2 cách hiểu: (a) `report_viewer` cần thấy được `CategoriesPage` (ít nhất tab "Vật tư", chỉ xem không có nút ghi — cơ chế `canWrite` ở trên đã sẵn sàng cho việc này); (b) "danh sách vật tư" trong overview chỉ ý nói cột tên/mã vật tư đã hiển thị sẵn trong `InventoryPage` (viewer đã thấy được), không cần thêm quyền vào trang riêng. Chưa tự quyết — hỏi lại người dùng ở lần xác nhận Giai đoạn C.
- Ảnh hưởng tới: Web (từ Giai đoạn E trở đi, khi viết form Thêm/Sửa/Xoá thật cho từng resource, PHẢI dùng `canWrite(resource)` đúng tên resource — không dùng `canAccess(page)` để quyết định hiện nút ghi cho trang nào gộp ≥2 resource).

## 2026-09-12 — Chốt phương án (a): report_viewer được XEM CategoriesPage (read-only)
- Bối cảnh: tiếp nối phát hiện ở mục trên — người dùng chọn phương án (a).
- Quyết định: `PAGE_ACCESS.categories` (`config/permissions.ts`) thêm `report_viewer`, áp dụng cho CẢ 2 tab (Danh mục + Vật tư — không tách quyền xem riêng theo tab vì đọc luôn là `ALL_ROLES` ở Backend cho cả 2 resource). **Diễn giải chính thức cho `00-OVERVIEW.md` mục 6 kể từ đây**: "Người xem báo cáo: ...danh sách vật tư/thiết bị (read-only)" nghĩa là được xem `CategoriesPage` (danh mục cha-con + bảng vật tư đầy đủ min/max/đơn vị/trạng thái) ở chế độ chỉ đọc — KHÔNG phải chỉ tên/mã vật tư hiển thị sẵn trong `InventoryPage`. Ghi lại rõ ở đây để các lần đọc `00-OVERVIEW.md` sau không hiểu nhầm thành phương án (b).
- **Phát hiện phụ trong lúc sửa — đã fix luôn**: `CategoriesPage.tsx` có 3 nút bút chì (sửa) ở cấp dòng (2 nút trong cây danh mục cha/con + 1 nút trong bảng vật tư) trước đó KHÔNG hề được bọc bởi bất kỳ điều kiện quyền nào (khác với nút "Thêm danh mục"/"Thêm vật tư" đã bọc `canWriteCurrentTab` từ trước) — nếu không sửa, thêm `report_viewer` vào `PAGE_ACCESS.categories` sẽ khiến viewer thấy 3 nút sửa này dù không có quyền ghi, sai với chính yêu cầu "0 nút hành động nào hiển thị". Đã bọc cả 3 nút bằng `canWriteCurrentTab` (dùng chung biến đã có, đúng theo resource `item_categories`/`items` tương ứng tab). Đã rà soát nhanh các trang khác có cùng icon bút chì (`RolesPage`, `UsersPage`, `SuppliersPage`) — cả 3 đều đã bị chặn ở cấp `PAGE_ACCESS` (admin-only hoặc admin+manager) nên viewer/staff không bao giờ chạm tới, không phải sửa thêm.
- **Đã verify bằng Playwright thật**: đăng nhập `viewer.test@warehouse.local` → sidebar hiện đúng mục "Danh mục vật tư" → vào trang, tab "Danh mục": thấy đủ dữ liệu cây danh mục (145/312/89/56 vật tư từng nhóm), **0** nút "Thêm danh mục", **0** nút bút chì sửa. Tab "Vật tư": thấy đủ 8 dòng vật tư, **0** nút "Thêm vật tư", **0** nút bút chì sửa. Không lỗi console.
- Ảnh hưởng tới: Web (Giai đoạn E khi viết CRUD thật cho `item_categories`/`items`, form thêm/sửa PHẢI tự kiểm tra lại `canWrite()` phía client lẫn dựa vào Backend chặn 403 — không tin tưởng riêng việc ẩn nút ở UI); nguyên tắc rút ra cho các trang sau: bất kỳ nút/icon hành động (kể cả nút sửa nhỏ ở từng dòng bảng, không chỉ nút "Thêm" ở đầu trang) PHẢI được bọc `canWrite()` ngay khi viết, không để tới lúc mở rộng quyền xem mới phát hiện thiếu.

## 2026-09-12 — Giai đoạn D: Dashboard nối API thật
- Bối cảnh: Backend không có endpoint tổng hợp riêng cho Dashboard (đúng như D1 đã lường trước trong `08-WEB-BUILD-CHECKLIST.md`) — phải gọi kết hợp nhiều API rồi tự tính ở FE.
- **QueryClientProvider chưa từng được mount** dù `@tanstack/react-query` đã cài từ Giai đoạn A1 — bổ sung ở gốc `App.tsx` (bọc ngoài `BrowserRouter`), `defaultOptions.queries` = `retry:1, staleTime:30_000`.
- Service mới (đều qua `apiClient.ts#request()` theo đúng quy ước Giai đoạn A): `inventory.service.ts`, `stockMovement.service.ts`, `warehouse.service.ts`, `item.service.ts`. Riêng 6 loại phiếu nghiệp vụ (import/export/transfer/recovery/stocktake/liquidation) dùng CHUNG 1 hàm `orders.service.ts#listOrders(resource, filters)` thay vì 6 file gần như giống hệt nhau — vì cả 6 route `GET /api/<resource>?status=` có hình dạng response giống hệt nhau (đã đối chiếu `backend/src/routes/*.js`); khi Giai đoạn E-F cần `create`/`confirm` riêng từng loại, sẽ tách file riêng lúc đó (có thể tái dùng `listOrders` bên trong).
- `hooks/useDashboardStats.ts` — 1 hook gộp toàn bộ: `useQuery` cho items(status=ACTIVE)/inventory/warehouses/stock-movements(30 ngày gần nhất), `useQueries` cho 6 lời gọi phiếu song song (`status=DRAFT`). Tính toán ở client:
  - **Tổng vật tư** = số `items` trả về (status=ACTIVE).
  - **Sắp hết hàng** = số dòng `inventory` có `available_quantity <= item.min_stock` (đánh giá theo TỪNG DÒNG tồn kho, không gộp theo item — nhất quán với cách `InventoryPage` sẽ làm ở Giai đoạn E).
  - **Phiếu chờ xử lý** = tổng số phiếu `status=DRAFT` cộng dồn từ cả 6 loại nghiệp vụ. Đổi nhãn từ "Phiếu chờ **duyệt**" (mock cũ, ngụ ý có bước duyệt riêng) thành "Phiếu chờ **xử lý**" — khớp đúng thực tế Backend chỉ có DRAFT→CONFIRMED (không có bước duyệt trung gian, xem quyết định Giai đoạn B về `OrderStatus`).
  - **Số kho hoạt động** = số `warehouses` có `status=ACTIVE` / tổng số kho (Backend không hỗ trợ filter status cho warehouses, tự lọc ở client).
  - **Biểu đồ Nhập/Xuất 7 ngày** = lọc `stock_movements` (30 ngày) theo đúng `movement_date` từng ngày, cộng dồn `quantity` riêng cho `movement_type IMPORT` và `EXPORT` — nhãn trục X đổi từ "Tuần 36 cố định" (mock) thành 7 ngày lịch thật gần nhất (`T2`..`CN` tính đúng theo `Date.getDay()`).
  - **Hoạt động gần đây** = 5 dòng `stock_movements` mới nhất (Backend luôn trả cũ→mới, tự đảo mảng ở client — đúng note đã ghi ở Giai đoạn E7). Phát hiện chi tiết cần xử lý: cột `quantity` trong DB LUÔN dương cho IMPORT/EXPORT/TRANSFER_IN/TRANSFER_OUT/RECOVERY/LIQUIDATION (dấu +/- chỉ có Ý NGHĨA qua `movement_type`, xác nhận bằng đọc trực tiếp `exportOrder.service.js` — `recordMovement` nhận thẳng `line.quantity` dương); riêng `ADJUSTMENT_STOCKTAKE` lưu `quantity` ĐÃ CÓ DẤU sẵn (theo quyết định Giai đoạn E5). Viết hàm `signedQuantity()` xử lý đúng 2 trường hợp này để hiển thị +/- chính xác trong câu mô tả hoạt động — nếu không tách riêng sẽ hiển thị sai dấu cho kiểm kê.
  - Không tạo thêm "cảnh báo tồn kho thấp" độc lập — dùng lại đúng danh sách `Sắp hết hàng` (4 dòng đầu) cho card "Cảnh báo tồn kho thấp" thay vì mock riêng 4 vật tư cứng.
- Loại bỏ hoàn toàn mock: `weeklyData`, `recentActivity`, `stats` cứng, câu chào cứng "Cao Xuân Khu" (đổi sang `useAuth().user.full_name` thật) trong `DashboardPage.tsx`. Thêm trạng thái loading (spinner) + lỗi (`EmptyState`) khi API thất bại — trước đó Dashboard mock không có khái niệm loading vì dữ liệu luôn có sẵn ngay.
- **Tạo dữ liệu thật qua API** (không insert tay DB, giữ nguyên tắc như Giai đoạn C) để có dữ liệu thật kiểm thử Dashboard: 1 danh mục + 2 vật tư (`TEST-IC555` min_stock=50, `TEST-RELAY`), 2 kho (`Kho A Test` ACTIVE, `Kho B Test Inactive` INACTIVE — cố ý để test đúng số kho hoạt động ≠ tổng số kho), 1 vị trí, 1 NCC, nhập kho 100 + xuất kho 60 (đã confirm, để lại tồn 40 < ngưỡng 50 → kích hoạt cảnh báo sắp hết hàng thật), thêm 1 phiếu nhập + 1 phiếu xuất để DRAFT (không confirm) để test đếm "phiếu chờ xử lý". **Giữ lại** dữ liệu này (không dọn sau khi test, khác với các script tạm ở Backend) — cùng tinh thần với 3 tài khoản test đã tạo ở Giai đoạn C, dùng làm dữ liệu thật cho test thủ công H1/H3 sau này thay vì phải tạo lại từ đầu.
- **Đã verify bằng Playwright thật** (dựng cả Backend+Web thật, đăng nhập admin): 4 stat card hiện đúng số liệu tính từ dữ liệu vừa tạo (Tổng vật tư=2, Sắp hết hàng=1, Phiếu chờ xử lý=2 "1 nhập kho, 1 xuất kho", Số kho hoạt động=1/tổng 2); biểu đồ hiện đúng cột ngày hôm nay (nhập 100 xanh dương, xuất 60 tím) + đúng tổng "Tổng nhập: 100 đơn vị / Tổng xuất: 60 đơn vị"; feed hoạt động hiện đúng thứ tự MỚI→CŨ (Xuất kho -60 trước, Nhập kho +100 sau) kèm tên kho/vị trí/người thực hiện thật; cảnh báo tồn kho thấp hiện đúng "IC555 Test 40/50 Cai". Không lỗi console.
- Ảnh hưởng tới: Web (Giai đoạn E-G khi viết các trang CRUD/nghiệp vụ khác, dùng lại đúng các service đã tạo ở đây — `inventory.service.ts`/`stockMovement.service.ts`/`item.service.ts`/`warehouse.service.ts`/`orders.service.ts` — không viết lại logic gọi API trùng lặp; mọi query key React Query nên đặt tên nhất quán kiểu `['resource', filters]` như đã làm ở `useDashboardStats.ts` để cache/invalidate đúng khi Giai đoạn E thêm mutation tạo/sửa/xoá).

## 2026-09-13 — Giai đoạn E1+E2: CRUD thật Danh mục vật tư + Vật tư
- Bối cảnh: `CategoriesPage.tsx` (2 tab, Giai đoạn B/C đã gate quyền ghi qua `canWrite()`) chuyển từ mock sang API thật đầu tiên trong loạt CRUD — mọi nghiệp vụ sau (F) đều phụ thuộc `item_id` nên làm trước.
- **Phát hiện field không khớp schema**: mock Figma Make có field `code` (IC/LKT/MOD...) hiển thị badge 2 ký tự cho mỗi danh mục — `ItemCategory` model THẬT không có field này (`category_id`, `category_name`, `parent_id`, `description`). Xác nhận bằng cách gọi thật `POST /api/item-categories` kèm `category_code` — Zod tự động strip field lạ, response không có field đó. Đã bỏ hẳn field `code` khỏi UI thật, badge 2 ký tự giờ lấy 2 ký tự đầu của `category_name` thay vì 1 field riêng không tồn tại (đúng nguyên tắc "không tự bịa field ngoài schema" — `03-WEB-SPEC.md` mục 3).
- Service mới: `itemCategory.service.ts` (list/create/update/remove), mở rộng `item.service.ts` (Giai đoạn D mới có `list`) thêm `create/update/remove` + field `category` (nested object, trước đó thiếu trong type).
- Thêm `ConfirmDialog` dùng chung vào `components/ui.tsx` (dựng trên `Modal` có sẵn) — dùng cho MỌI xác nhận Xoá từ đây về sau, tránh mỗi trang tự viết `window.confirm()` hoặc modal xác nhận riêng.
- Form Thêm/Sửa dùng `useState` thường (không dùng `react-hook-form` dù đã cài từ Giai đoạn A1) — khớp quy ước có sẵn từ `UsersPage.tsx` (mock cũ), đủ đơn giản cho 2 form này (≤6 field, không có field-array). Cân nhắc dùng `react-hook-form` khi Giai đoạn F cần form nhiều dòng động (bảng vật tư trong 1 phiếu).
- **RBAC cấp nút hành động**: tái sử dụng đúng `canWriteCurrentTab` đã có từ Giai đoạn C (theo `canWrite('item_categories')`/`canWrite('items')` tương ứng tab) — bọc cả nút "Thêm" (đã có sẵn) lẫn 2 nút Sửa/Xoá mới thêm ở từng dòng.
- **Phát hiện quan trọng về hành vi XOÁ danh mục — khác với kỳ vọng ban đầu**: test xoá 1 danh mục CÒN danh mục con (parent-child) → xoá THÀNH CÔNG (không 409), danh mục con tự động bị gỡ quan hệ (`parent_id` → `null`), KHÔNG bị chặn. Truy vào `backend/prisma/migrations/20260911095813_init/migration.sql` xác nhận: FK `item_categories_parent_id_fkey` là `ON DELETE SET NULL` (quan hệ cha-con là optional), trong khi FK `items_category_id_fkey` (Item → Category) là `ON DELETE RESTRICT` (bắt buộc, không optional). Tức là: **chỉ có item mới chặn xoá category (409) — category con KHÔNG chặn xoá category cha** (children tự "mồ côi", không lỗi). `06-BUILD-CHECKLIST.md` C2 chỉ test đúng trường hợp item→category (409) nên không phát hiện ra khác biệt này trước đây. Đây là hành vi THẬT của DB, không phải bug — nhưng là 1 UX cần lưu ý: xoá 1 danh mục cha đang có danh mục con sẽ ÂM THẦM làm mồ côi các con đó (không có cảnh báo riêng). Tạm thời CHƯA thêm cảnh báo phía FE cho trường hợp này (ngoài phạm vi E1/E2 đã yêu cầu) — nếu cần, có thể bổ sung sau bằng cách kiểm tra `childrenOf(id).length > 0` trước khi cho xoá và hiện cảnh báo riêng.
- **Đã verify bằng Playwright thật** (đăng nhập thật, dựng Backend+Web thật):
  1. **CRUD đầy đủ** (role admin): tạo danh mục → tạo vật tư trong danh mục đó → sửa danh mục (đổi tên) → tất cả đều thành công, dữ liệu thật đúng qua network log (`POST`/`PUT` đúng payload, đúng ID đích).
  2. **RBAC nút hành động**: `report_viewer` vào `/categories` — thấy đủ dữ liệu 2 tab, **0** nút Thêm/Sửa/Xoá nào. `warehouse_staff` gõ thẳng `/categories` — bị chặn ở tầng route ("Không có quyền truy cập"), đúng vì `staff` không nằm trong `PAGE_ACCESS.categories`.
  3. **DELETE đúng hành vi + lỗi 409 hiển thị đúng trên UI** (không phải màn trắng/lỗi console): tạo danh mục + 1 vật tư thuộc danh mục đó → xoá danh mục → toast đỏ hiện đúng nguyên văn message Backend "Dữ liệu tham chiếu không hợp lệ hoặc đang được sử dụng bởi bản ghi khác" (chụp ảnh xác nhận). Soft-delete vật tư đó (chuyển `Ngưng dùng`, vẫn hiện trong danh sách — đúng hành vi soft-delete, KHÔNG biến mất) → thử xoá lại danh mục → **vẫn bị chặn 409** (đúng, vì soft-delete không xoá thật dòng item, FK vẫn còn) — xác nhận soft-delete không "mở khoá" được xoá category.
  - 1 dòng "error" log console là chính trình duyệt tự ghi lại response 409 (network log level, không phải lỗi JS runtime) — không phải bug, không ảnh hưởng UX (toast đã hiển thị đúng, `apiClient` đã bắt và xử lý bình thường).
- Dữ liệu test tạo trong lúc verify (giữ lại theo tinh thần Giai đoạn C/D): danh mục "Cat With Item" (còn 1 vật tư `CRUD-E1-001` đã soft-delete tham chiếu — cố ý giữ để minh hoạ hành vi 409 cho ai xem lại sau), danh mục "Linh kien Test D" (đã có từ Giai đoạn D).
- Ảnh hưởng tới: Web (Giai đoạn E3-E6 dùng lại đúng pattern này — service theo resource, `ConfirmDialog` dùng chung, `canWrite()` bọc từng nút, không phải Playwright đầy đủ 4 vai trò nữa mà chỉ code review + 1 lần gọi API thật theo yêu cầu mới của người dùng); nếu sau này cần chặn xoá category-có-con ở FE, thêm kiểm tra `childrenOf(id).length` trước khi mở `ConfirmDialog`.

## 2026-09-13 — Giai đoạn E3-E6: CRUD thật Kho + Vị trí + NCC + Tồn kho (mức kiểm thử nhẹ)
- Bối cảnh: người dùng chốt quy tắc mới — CRUD lặp lại đúng khuôn đã kiểm chứng ở E1/E2 (cùng RBAC 2 nhóm role, cùng kiểu soft-delete) chỉ cần code review + 1 lần gọi API thật, không cần Playwright + ảnh đầy đủ 4 vai trò như E1/E2. Áp dụng đúng tinh thần đó cho E3-E6, gộp báo cáo chung.
- Xác nhận qua code review (`backend/src/routes/*.js`, `*.service.js`): cả 3 resource (`warehouses`, `storage_locations`, `suppliers`) đều DELETE = soft-delete (status→INACTIVE), giống hệt pattern `items` ở E1/E2 — không có trường hợp hard-delete/409 đặc biệt nào cần test riêng như category (đã test kỹ ở E1).
- Service mới/mở rộng: `warehouse.service.ts` (thêm create/update/remove, trước đó Giai đoạn D chỉ có `list`), `storageLocation.service.ts` (mới, đủ CRUD, filter `warehouse_id`), `supplier.service.ts` (mới, đủ CRUD).
- `WarehousesPage.tsx` (E3+E4): nối API thật cả 2 tab, tái sử dụng đúng `canWrite('warehouses')`/`canWrite('storage_locations')` đã có từ Giai đoạn C (bug tab-permission đã fix ở đó, giờ áp dụng luôn cho nút Sửa/Xoá mới thêm, không chỉ nút Thêm). Bỏ field mock không tồn tại schema thật: `type`(main/sub), `used`/`locations`(đếm tay) — đếm số vị trí thật bằng `locationCountOf()` tính từ dữ liệu `storage_locations` thật thay vì field cứng.
- `SuppliersPage.tsx` (E5): nối API thật. Bỏ các field mock không có trong schema (`category` nhóm hàng, `rating` đánh giá sao, `orders` số đơn hàng, `short` tên viết tắt) — không tự bịa thêm field. Bỏ luôn 2 nút "Tạo đơn mua"/"Lịch sử" ở panel chi tiết vì không có hành động thật đứng sau (cùng loại vấn đề với nút giả đã xử lý trước đây).
- `InventoryPage.tsx` (E6): nối API thật `GET /api/inventory` (chỉ đọc — không có route ghi, đúng vì tồn kho chỉ đổi gián tiếp qua nghiệp vụ Giai đoạn F). Trạng thái (Bình thường/Sắp hết/Thiếu hàng) tính lại ở client theo từng DÒNG tồn kho (`available_quantity` so với `item.min_stock`, `<=0` → Thiếu hàng) — nhất quán với cách `useDashboardStats.ts` đã tính ở Giai đoạn D. Bỏ field mock không có trong response thật của endpoint này (`category`, `supplier`, `lastImport` — object `item` trong `GET /api/inventory` không include các field đó, khác `GET /api/items`).
- **Đã verify** (mức nhẹ theo đúng thoả thuận — Playwright smoke test 1 lượt, KHÔNG lặp lại đủ 4 vai trò vì đã xác nhận cùng cơ chế `canWrite()`/`PAGE_ACCESS` ở Giai đoạn C và E1/E2): đăng nhập admin, tạo thật 1 kho + 1 vị trí + 1 NCC qua UI (đều ra toast thành công), soft-delete thử 1 kho (ra đúng toast + set INACTIVE), vào Tồn kho xác nhận hiện đúng dữ liệu thật (1 dòng tồn kho `IC555 Test` 40/50 tại Kho A Test — đúng dữ liệu đã tạo ở Giai đoạn D) + lọc theo từ khoá "IC555" ra đúng 1 kết quả. Không lỗi console trong toàn bộ lượt test.
- Ảnh hưởng tới: Web (Giai đoạn F — 6 nghiệp vụ kho — sẽ là logic mới/phức tạp thật đầu tiên kể từ khi đổi mức kiểm thử, GIỮ NGUYÊN mức kiểm tra kỹ như E1/E2 kể cả có Playwright + ảnh chụp đầy đủ, đặc biệt các trường hợp có transaction/rollback đã test kỹ ở Backend Giai đoạn E).

## 2026-09-13 — Giai đoạn F1+F2: Nhập kho (thường + AI thật) và Xuất kho (mức kiểm thử ĐẦY ĐỦ)
- Bối cảnh: đây là logic mới/phức tạp đầu tiên từ khi đổi mức kiểm thử (transaction thật, luồng AI, rollback) — giữ nguyên mức kiểm tra kỹ (Playwright + ảnh chụp) theo đúng thoả thuận, không áp dụng mức nhẹ của E3-E6.
- **Phát hiện gap so với spec/checklist trước khi code xong**: `03-WEB-SPEC.md` mục 2 + `08-WEB-BUILD-CHECKLIST.md` F1 yêu cầu CẢ "form tạo phiếu thường" LẪN "luồng AI đầy đủ", nhưng bản mock Figma gốc (`ImportPage.tsx`) chỉ có 1 luồng duy nhất (AI wizard 3 bước, không có form thường). Bản build đầu tiên của tôi kế thừa nguyên gap này (chỉ dựng lại luồng AI). Tự phát hiện khi đối chiếu lại checklist trước khi tick — đã bổ sung thêm luồng tạo phiếu THƯỜNG (nút "Tạo phiếu nhập" riêng, tách khỏi nút "Nhập kho bằng AI") trước khi coi F1 hoàn tất.
- **Quyết định kiến trúc 2 luồng tạo phiếu nhập, dùng 2 API khác nhau (không gộp)**:
  - Luồng thường (`manualForm`/`manualRows` trong `ImportPage.tsx`): gọi `POST /import-orders` (create thường) → phiếu ở `DRAFT` → xác nhận sau, riêng, qua nút "Xác nhận nhập kho" ở modal chi tiết (gọi `POST /import-orders/:id/confirm`).
  - Luồng AI: giữ đúng quyết định đã chốt tại mục "F2: gộp create+confirm trong 1 transaction cho luồng AI" (2026-09-11) — sau khi người dùng xác nhận danh sách ở bước 2, gọi THẲNG `POST /import-orders/from-ai` (tạo + confirm cùng 1 transaction, không có bước "Xác nhận" riêng, không bao giờ để phiếu AI ở trạng thái DRAFT lộ ra ngoài).
  - Lý do tách 2 API thay vì dùng chung 1 luồng cho cả 2 trường hợp: khớp đúng thiết kế Backend đã có sẵn (2 route riêng `create` và `createFromAi`), và đúng tinh thần "AI là luồng có UX khác — người dùng đã tự duyệt kết quả AI trước khi bấm nút cuối, không cần duyệt 2 lần".
- **Luồng AI nối API thật, không còn bịa toạ độ/overlay**: `ai.service.ts` (`detectImage()`) gọi thật `POST /ai/detect` (multipart field `image`), nhận đúng cấu trúc đã chốt (`detections`/`summary`/`annotated_image`). Bounding box vẽ đè lên ảnh bằng % tính từ tỉ lệ `bounding_box.{x,y,width,height}` chia cho `naturalWidth`/`naturalHeight` thật của `annotated_image` (bắt qua sự kiện `onLoad` của thẻ `<img>`) — chính xác theo dữ liệu Backend trả về, không vẽ toạ độ cố định giả như bản mock cũ.
- **Vấn đề mapping class_name → item thật, xử lý ở UI**: mock AI trả `class_name` chung chung ("resistor", "ic_chip"), KHÔNG map sẵn tới `item_id` nào trong danh mục thật. Bước 2 (bảng xác nhận) bắt buộc người dùng tự chọn 1 vật tư thật + 1 vị trí thật cho MỖI nhóm `summary`, số lượng mặc định = `count` AI trả nhưng cho sửa tay — không tự ý suy luận mapping (đúng nguyên tắc "không tự bịa dữ liệu").
- **Xuất kho (`ExportPage.tsx`)**: bỏ 3 nút giả của mock cũ ("Lưu nháp"/"Gửi duyệt"/"Xuất kho ngay" — 2 nút đầu trên thực tế cùng 1 hành vi DRAFT nên gộp), thay bằng đúng 2 hành động THẬT có ý nghĩa khác nhau ở Backend: "Lưu nháp" (chỉ `POST /export-orders`) và "Xuất kho ngay" (create rồi gọi `confirm` ngay). Cột "Tồn khả dụng" tra `GET /inventory?warehouse_id=` để cảnh báo SỚM (trước khi submit) nhưng KHÔNG tự chặn submit — quyết định thật (chặn hay không) luôn để Backend `decrementInventory()` enforce ở bước confirm, tránh trường hợp UI chặn sai lệch so với dữ liệu tồn kho có thể đã đổi giữa lúc người dùng nhập form.
- **Đã verify bằng Playwright thật, Backend+DB thật** (dùng data thật: kho "Kho A Test", vị trí "A1-TEST", vật tư "TEST-RELAY"/"TEST-IC555"):
  1. Luồng AI: upload file fixture thật `backend/postman/fixtures/fake-image.jpg` → `/ai/detect` trả đúng 2 nhóm mock cố định (resistor×5, ic_chip×2) → map + chọn vị trí → `/from-ai` tạo phiếu `IMP-...` trạng thái CONFIRMED NGAY (đúng, list/detail không có nút "Xác nhận" vì đã confirmed) → verify `GET /inventory`: RELAY +5→10, IC555 +2→44 (thật, không phải optimistic-update giả).
  2. Luồng thường: tạo phiếu DRAFT (RELAY +3 vị trí A1-TEST) → verify list hiện đúng badge "Nháp" → bấm "Xác nhận nhập kho" ở modal chi tiết → verify tồn kho THẬT tăng đúng +3 (10→13) sau khi confirm, KHÔNG tăng lúc tạo DRAFT (đúng — chỉ `confirm()` mới gọi `incrementInventory`, khớp code `importOrder.service.js` Backend).
  3. Xuất kho — kịch bản LỖI thật: tạo phiếu xuất RELAY số lượng 999999 tại A1-TEST (đang có 10) → bấm "Xuất kho ngay" → toast đỏ hiện ĐÚNG NGUYÊN VĂN message Backend `"Tồn kho không đủ để xuất (còn 10, cần 999999) tại vị trí đã chọn"` → verify ROLLBACK thật: gọi lại `GET /inventory` xác nhận RELAY vẫn = 10 (không đổi) — đúng transaction Prisma (`decrementInventory` throw bên trong `$transaction` làm rollback toàn bộ, kể cả các dòng khác trong cùng phiếu nếu có). Phiếu vẫn tồn tại ở list với trạng thái DRAFT (create thành công, chỉ confirm thất bại) — không mất dữ liệu, đúng hành vi thật.
  4. Xuất kho — kịch bản THÀNH CÔNG: xuất 1 đơn vị IC555 tại A1-TEST → toast xanh "Đã xuất kho thành công" → verify tồn kho giảm đúng 1 (44→43).
  - Không có `pageerror` ở bất kỳ kịch bản nào. 1 dòng "console error: Failed to load resource...400" ở kịch bản (3) là log mạng bình thường của chính request `confirm` bị Backend từ chối có chủ đích (không phải lỗi JS runtime, không phải bug).
- **Ghi chú môi trường test (không liên quan code)**: máy chạy Playwright bản Chromium đầy đủ (`chromium-1193`) bị lỗi/kẹt khi tải+giải nén nhiều lần (nghi do bị kill nhầm khi dọn tiến trình node rác, hoặc do hạ tầng chặn). Theo quyết định của người dùng, chuyển hẳn sang dùng `chromium_headless_shell-1243` (đã cài sẵn, ổn định) làm `executablePath` khi `chromium.launch()` cho MỌI script test Playwright từ giờ về sau trong dự án này — vẫn chạy được `page.screenshot()`, `setInputFiles()`, network/console listener đầy đủ, chỉ khác là không mở được cửa sổ có giao diện thật (`headless: false`) để mắt thường xem trực tiếp, không ảnh hưởng gì tới việc kiểm thử tự động.
- Ảnh hưởng tới: Web (F3-F6 — Chuyển kho/Thu hồi/Kiểm kê/Thanh lý — nếu cũng có luồng "tạo phiếu thường", áp dụng đúng pattern DRAFT→confirm-riêng như Import/Export ở đây; nếu bất kỳ nghiệp vụ nào có ràng buộc số lượng giống `decrementInventory`, áp dụng đúng nguyên tắc "UI chỉ cảnh báo sớm, Backend enforce thật ở bước confirm" đã dùng ở Export); mọi script Playwright sau này trong dự án PHẢI truyền `executablePath` trỏ tới `chromium_headless_shell-1243/chrome-headless-shell-win64/chrome-headless-shell.exe` khi gọi `chromium.launch()`.

## 2026-09-13 — Giai đoạn F3+F4: Chuyển kho (2 vị trí nguồn/đích) và Thu hồi (mức kiểm thử ĐẦY ĐỦ)
- Bối cảnh: tiếp tục giữ mức kiểm thử đầy đủ (Playwright + Backend/DB thật) vì Chuyển kho là transaction phức tạp thứ 2 (decrement nguồn + increment đích + 2 dòng `stock_movements` TRANSFER_OUT/TRANSFER_IN trong cùng 1 transaction — xem `transferOrder.service.js`), Thu hồi đơn giản hơn (chỉ increment 1 chiều, giống hệt cấu trúc `recoveryOrder.service.js` ~ `importOrder.service.js` bớt phần AI).
- **Yêu cầu riêng của người dùng cho F3**: giao diện phải thể hiện RÕ 2 vị trí nguồn/đích, không để nhầm lẫn. Giải pháp: tách biệt ở CẢ 2 CẤP — (1) cấp kho: 2 Card viền màu khác nhau (`border-indigo-100` "Kho nguồn" / `border-green-100` "Kho đích", giữ đúng bố cục 2 cột + mũi tên ở giữa từ mock gốc); (2) cấp vị trí trong bảng vật tư: 2 cột Select riêng "Vị trí nguồn"(header tô `text-indigo-500`)/"Vị trí đích"(header tô `text-green-600`), mỗi Select CHỈ hiện danh sách vị trí thuộc đúng kho tương ứng đã chọn (`fromLocations`/`toLocations` lọc riêng theo `from_warehouse_id`/`to_warehouse_id`) — không thể chọn nhầm vị trí của kho kia vì nó không xuất hiện trong danh sách. Modal chi tiết phiếu và bảng vật tư dùng lại đúng 2 màu này (mã vị trí nguồn nền indigo, đích nền green) để nhất quán xuyên suốt.
- `transferOrder.service.ts`/`recoveryOrder.service.ts`: theo đúng khuôn `importOrder.service.ts`/`exportOrder.service.ts` đã có — `list`/`create`/`confirm`, không có endpoint từ-AI (không áp dụng cho 2 nghiệp vụ này).
- `TransferPage.tsx`/`RecoveryPage.tsx`: tái sử dụng đúng pattern 2 nút thật đã dùng ở Export ("Lưu nháp" = create thường DRAFT; nút hành động chính = create rồi gọi `confirm` ngay) — KHÔNG có luồng AI (không áp dụng cho 2 nghiệp vụ này, chỉ Nhập kho mới có AI theo đúng phạm vi spec/quyết định F1).
- **Đã verify bằng Playwright thật, Backend+DB thật** (tạo thêm fixture "Kho B Test 2" + vị trí "B1-TEST" làm kho đích thứ 2, vì trước đó chỉ có 1 kho ACTIVE — giữ lại theo tinh thần dữ liệu test đã dùng xuyên suốt dự án):
  1. Chuyển kho — kịch bản LỖI thật: chuyển TEST-RELAY từ Kho A Test/A1-TEST (đang có 13) sang Kho B Test 2/B1-TEST với số lượng 999999 → "Chuyển kho ngay" → toast đỏ hiện ĐÚNG NGUYÊN VĂN message Backend `"Tồn kho không đủ để xuất (còn 13, cần 999999) tại vị trí đã chọn"` (dùng lại đúng `decrementInventory` — chuyển kho cũng decrement nguồn y hệt xuất kho) → phiếu vẫn ở DRAFT trong danh sách (create thành công, chỉ confirm thất bại, không rollback việc TẠO phiếu — chỉ rollback phần thay đổi tồn kho bên trong transaction confirm).
  2. Chuyển kho — kịch bản HỢP LỆ: chuyển 2 đơn vị → verify qua `GET /inventory`: nguồn giảm đúng 13→11, đích tăng đúng 0→2 (tự tạo dòng inventory mới ở đích vì trước đó chưa từng có tồn kho ở vị trí B1-TEST — đúng hành vi `incrementInventory` khi chưa tồn tại dòng nào, upsert-create).
  3. Thu hồi: thu hồi 5 đơn vị TEST-IC555 vào Kho A Test/A1-TEST → verify tồn kho tăng đúng 43→48. Đơn giản, không có kịch bản lỗi cần test riêng (không có ràng buộc số lượng nào ở chiều tăng).
  - Không có `pageerror` ở mọi kịch bản.
- Ảnh hưởng tới: Web (F5 Kiểm kê — có cơ chế khác hẳn, dùng `setInventoryQuantity` SET tuyệt đối thay vì cộng/trừ tương đối, không có ràng buộc `available_quantity` như Export/Transfer — cần đọc kỹ `stocktakeSession.service.js` trước khi code UI, không áp dụng máy móc pattern "cảnh báo sớm + Backend enforce ở confirm" của Export/Transfer vì bản chất phép tính khác hẳn; F6 Thanh lý — kiểm tra xem có decrement giống Export hay chỉ đổi status như soft-delete trước khi thiết kế UI).

## 2026-09-13 — Giai đoạn F5+F6: Kiểm kê (SET tuyệt đối, kịch bản "nhiễu") và Thanh lý — HOÀN TẤT Giai đoạn F
- Bối cảnh: 2 nghiệp vụ phức tạp nhất còn lại. F5 đặc biệt rủi ro vì cơ chế SET tuyệt đối (`setInventoryQuantity`) hoàn toàn khác cộng/trừ tương đối của F1-F4 — người dùng yêu cầu bắt buộc phải mô phỏng kịch bản "nhiễu" (nghiệp vụ khác xảy ra xen giữa lúc mở phiên và lúc confirm) để chứng minh UI hiển thị và hành xử đúng với bản chất thật của Backend, không tự suy diễn sai thành phép cộng.
- **Đọc kỹ `stocktakeSession.service.js` trước khi code (theo đúng khuyến nghị đã ghi ở mục trước)**: xác nhận `create()` snapshot TOÀN BỘ dòng `inventory` hiện có của kho vào `system_quantity` NGAY tại thời điểm gọi API (không tự cập nhật sau đó); `confirm()` chỉ gọi `setInventoryQuantity(actual_quantity)` cho dòng có `difference !== 0` — nghĩa là nếu người kiểm kê nhập đúng bằng số snapshot cũ (không phát hiện chênh lệch), Backend SẼ BỎ QUA dòng đó (không ghi đè), nhưng nếu có chênh lệch thật (dù lý do gì), Backend SET THẲNG bằng `actual_quantity` — hoàn toàn không quan tâm tồn kho hiện tại (live) là bao nhiêu. Đây chính là rủi ro cần cảnh báo UI.
- **Thiết kế UI theo đúng cảnh báo của người dùng**:
  1. Banner vàng LUÔN hiển thị (không phải tuỳ chọn ẩn/hiện) ngay khi vào màn kiểm kê, nêu rõ 3 ý: (a) xác nhận = SET tuyệt đối, không phải cộng/trừ; (b) cột "Hệ thống" là ảnh chụp nhanh tại thời điểm MỞ phiên, không phải số liệu sống; (c) nghiệp vụ khác xảy ra sau đó tại kho này sẽ bị ghi đè khi xác nhận.
  2. Nút "Xác nhận kiểm kê" bị **disable ở phía client** (thuộc tính `disabled` thật trên DOM, không chỉ dựa vào try/catch bắt lỗi 400 của Backend) khi còn bất kỳ dòng nào có `actual_quantity === null`, kèm đếm số dòng còn thiếu hiển thị ngay cạnh nút.
  3. Cột "Thực tế" cho phép nhập tự do (không giới hạn theo tồn kho hiện tại) — đúng bản chất kiểm kê là điều chỉnh cho khớp thực tế, không phải giao dịch xuất/nhập có ràng buộc `available_quantity`.
- **Đã verify bằng Playwright thật kịch bản "nhiễu" — mô phỏng ĐÚNG rủi ro Backend thật có** (không phải giả lập giả): TEST-RELAY tồn kho = 11 trước khi mở phiên → mở phiên (snapshot `system_quantity` = 11) → TRONG LÚC phiên còn DRAFT, gọi thật `POST /import-orders` + `confirm` cộng thêm 7 đơn vị (nghiệp vụ nhập kho thật xảy ra xen giữa, y hệt kịch bản người dùng mô tả) → tồn kho LIVE lúc này = 18 → người kiểm kê nhập `actual_quantity = 25` (số bất kỳ, khác cả snapshot 11 lẫn live 18) → Lưu tiến độ → Xác nhận kiểm kê → **kết quả tồn kho cuối cùng = 25 CHÍNH XÁC** — không phải 18 (nếu Backend "khôn" bỏ qua vì đã có thay đổi), không phải 32 (nếu Backend cộng dồn `live + difference`). Chứng minh RÕ RÀNG: UI hiển thị và hành xử ĐÚNG 100% theo hành vi SET tuyệt đối thật của Backend, kể cả khi điều đó có nghĩa là ghi đè mất dữ liệu của nghiệp vụ khác — đây KHÔNG PHẢI lỗi cần sửa (nằm ngoài phạm vi Web, là quyết định thiết kế của Backend `02-BACKEND-SPEC.md`/`stocktakeSession.service.js`), mà là rủi ro nghiệp vụ CẦN được người vận hành biết trước — banner cảnh báo đã làm đúng vai trò này.
- **Đã verify chặn client-side**: mở phiên xong (2 dòng, chưa nhập gì) → nút "Xác nhận kiểm kê" có `disabled` = true thật (kiểm tra DOM property, không phải chỉ ẩn bằng CSS), kèm text "Còn 2 dòng chưa nhập" → sau khi nhập đủ 2 dòng + bấm "Lưu tiến độ" thành công → nút tự chuyển `disabled` = false.
- **Thanh lý (`DisposalPage.tsx`, `liquidationOrder.service.ts`)**: theo đúng pattern Export/Transfer (create DRAFT + confirm riêng, "Lưu nháp"/"Thanh lý ngay"). Bỏ 2 field bịa của mock cũ (`Giá trị ước tính (VNĐ)`, `Hình thức xử lý`) vì `liquidationOrderItemSchema` thật chỉ có `item_id`/`location_id`/`quantity`/`note`, không có field giá trị/hình thức nào — không tự thêm field ngoài schema. RBAC dùng `MANAGE_ROLES` (admin+manager, KHÔNG có staff — khác hẳn F1-F4 dùng `STAFF_WRITE_ROLES`), đã có sẵn đúng trong `permissions.ts` từ trước (`stocktake`/`disposal` trong `PAGE_ACCESS`), không cần sửa.
- **Đã verify Thanh lý**: kịch bản thiếu tồn kho (999999, còn 48) → bị từ chối đúng message Backend, tồn kho không đổi; kịch bản hợp lệ (3 đơn vị) → tồn kho giảm đúng 48→45.
- **Đã verify RBAC F5+F6 riêng** (khác F1-F4 — đây là lần đầu có role bị loại hoàn toàn khỏi PAGE_ACCESS của nhóm nghiệp vụ kho, không chỉ report_viewer): đăng nhập `staff.test@warehouse.local`, gõ thẳng URL `/stocktake` và `/disposal` → cả 2 đều hiện đúng màn "Không có quyền truy cập"; sidebar cũng ẩn đúng 2 mục này cho staff (trong khi F1-F4 vẫn hiện đầy đủ với staff).
- Không có `pageerror` ở bất kỳ kịch bản nào trong toàn bộ F5+F6.
- **TỔNG KẾT GIAI ĐOẠN F — đối chiếu `03-WEB-SPEC.md` mục 2 (6 nghiệp vụ kho)**: cả 6 nghiệp vụ (Nhập/Xuất/Chuyển/Thu hồi/Kiểm kê/Thanh lý) đã nối API thật 100%, đúng đủ field theo `02-BACKEND-SPEC.md`/schema Prisma thật (không có field bịa), đúng luồng AI 3 bước cho Nhập kho theo mục 4 của spec (đã điều chỉnh: dùng thẳng `/from-ai` theo quyết định F2 thay vì tự-điền-vào-form-thường như spec gợi ý ban đầu — đã ghi rõ lý do ở mục Giai đoạn F1+F2 phía trên), RBAC đúng theo `00-OVERVIEW.md` mục 6 cho cả 2 nhóm role (STAFF_WRITE_ROLES cho F1-F4, MANAGE_ROLES cho F5-F6), trạng thái phiếu chuẩn hoá đúng 3 giá trị thật (DRAFT/CONFIRMED/CANCELLED — không còn 5 trạng thái bịa của mock Figma Make ban đầu như phát hiện ở Giai đoạn B). Chưa làm: cột "Người duyệt"/nút huỷ phiếu (CANCELLED) — Backend hiện KHÔNG có endpoint huỷ phiếu nào (không có route `PATCH`/`DELETE` cho 6 loại phiếu này ngoài `confirm`), nên trạng thái CANCELLED tồn tại trong enum nhưng không có luồng UI nào tạo ra nó — đúng thực tế, không tự bịa thêm nút "Huỷ phiếu" khi Backend chưa hỗ trợ.
- Ảnh hưởng tới: Web (Giai đoạn G — Lịch sử biến động kho sẽ cần đọc đúng `movement_type` đã dùng xuyên suốt Giai đoạn F: `IMPORT`/`EXPORT`/`TRANSFER_OUT`/`TRANSFER_IN`/`RECOVERY`/`LIQUIDATION`/`ADJUSTMENT_STOCKTAKE` — 7 loại, không phải 6, vì Chuyển kho ghi 2 dòng movement riêng biệt cho nguồn/đích); Mobile/AI Service (nếu có nghiệp vụ kiểm kê, PHẢI cảnh báo đúng rủi ro SET tuyệt đối này, không được thiết kế UI ngầm hiểu là cộng/trừ).

## 2026-09-13 — Giai đoạn G1-G3: Lịch sử biến động kho, Quản lý người dùng, Quản lý vai trò (mức kiểm thử theo rủi ro)
- Bối cảnh: người dùng chốt Giai đoạn G là CRUD/trang đọc dữ liệu, rủi ro thấp hơn Giai đoạn F → áp dụng lại mức nhẹ (code review + smoke test) như E3-E6, TRỪ RBAC của G2/G3 (Quản lý người dùng/vai trò) — quyền nhạy cảm nhất hệ thống — phải test kỹ.
- **G1 — Lịch sử biến động kho (`stockMovement.service.ts` đã có sẵn từ Giai đoạn D, chỉ viết `HistoryPage.tsx` mới)**:
  - Xác nhận qua code review: `stockMovementService.list()` (Backend) luôn `orderBy: { movement_date: 'asc' }` — CỐ Ý khác `desc` của mọi trang danh sách phiếu khác (Import/Export/Transfer/.../Stocktake đều `orderBy: created_at desc`). Quyết định giữ nguyên `asc` ở trang này (KHÔNG tự đảo mảng như `DashboardPage`/`useDashboardStats.ts` đã làm cho mục "Hoạt động gần đây" — 2 chỗ dùng cùng dữ liệu nhưng khác mục đích: Dashboard cần "mới nhất trước" cho một widget tóm tắt, còn trang Lịch sử đầy đủ này là nhật ký/sổ cái nên đọc theo trình tự thời gian thật tự nhiên hơn) — đúng yêu cầu của người dùng.
  - Bộ lọc thật gửi thẳng lên Backend (không lọc giả ở client): `item_id`, `warehouse_id`, `movement_type`, `from`, `to` — đúng 4 filter Backend hỗ trợ (`item/kho/loại nghiệp vụ/khoảng ngày` theo checklist). Không thêm filter `location_id`/`reference_type` (Backend có hỗ trợ nhưng ngoài phạm vi checklist, không cần thêm UI cho việc chưa yêu cầu).
  - Icon+màu riêng cho ĐỦ 7 `movement_type` (mock cũ chỉ có 6 loại gộp chung "transfer", không tách IN/OUT): IMPORT=green, EXPORT=red, TRANSFER_OUT=indigo, TRANSFER_IN=blue, RECOVERY=purple, ADJUSTMENT_STOCKTAKE=yellow, LIQUIDATION=gray — trùng khớp chính xác 7 màu `Badge` sẵn có (không cần thêm màu mới). Icon TRANSFER_IN/OUT là 2 hướng mũi tên khác nhau (trước đây mock dùng chung 1 icon "transfer" 2 chiều).
  - Tái dùng `signedQuantity()` (đã viết ở Giai đoạn D) để hiển thị đúng dấu +/-; export thêm `MOVEMENT_LABELS` (trước đó chỉ dùng nội bộ `useDashboardStats.ts`, không có `export`) để `HistoryPage.tsx` dùng chung, tránh định nghĩa nhãn 2 lần.
  - Bỏ nút "Xuất Excel" và "Xem phiếu gốc" của mock cũ — không có hành động thật đứng sau (không có endpoint export, không có route xem chi tiết theo `reference_type`+`reference_id` động cho 6 loại phiếu khác nhau).
  - Đã verify (smoke, đúng mức nhẹ): trang đầu hiện 8 bản ghi thật, lọc "Nhập kho" còn đúng 7 dòng, click 1 dòng mở đúng panel chi tiết.
- **G2 — Quản lý người dùng (`user.service.ts` + `UsersPage.tsx` mới)**: CRUD thật đầy đủ theo `backend/src/services/user.service.js` — `DELETE` là soft-delete thật (status→INACTIVE, giữ nguyên bản ghi), "Kích hoạt lại" tái sử dụng `PUT` đổi status về ACTIVE (Backend không có endpoint riêng cho việc này — `updateUserSchema` cho phép sửa `status` nên dùng chung route update, không cần thêm gì mới). Vai trò trong form lấy real-time từ `GET /roles` (không hard-code 4 role như mock cũ) — nếu sau này thêm role mới qua DB, form vẫn tự đúng.
  - Đã verify bằng Playwright thật: tạo tài khoản mới → sửa tên → khoá tài khoản (`ConfirmDialog`) → kích hoạt lại — cả 4 bước thành công thật (toast + gọi API thật, không phải optimistic update giả).
- **G3 — Quản lý vai trò (`role.service.ts` + `RolesPage.tsx` mới)**: CHỈ danh sách đúng đúng phạm vi checklist (`08-WEB-BUILD-CHECKLIST.md` G3 ghi "danh sách", không yêu cầu CRUD dù Backend có sẵn full CRUD cho `roles`) — không tự làm thêm ngoài phạm vi yêu cầu.
  - **Quyết định thiết kế quan trọng**: KHÔNG dùng lại danh sách quyền hạn viết tay như mock cũ (mảng `permissions` cứng, dễ lệch thực tế khi `config/permissions.ts` đổi mà quên sửa trang này) — thay vào đó đọc TRỰC TIẾP `PAGE_ACCESS`/`RESOURCE_WRITE_ACCESS` thật từ `config/permissions.ts` (nguồn phân quyền DUY NHẤT của toàn app) để tự liệt kê đúng những trang mỗi vai trò truy cập được + có ghi hay chỉ đọc, và đếm số người dùng thật theo từng `role_id` từ `GET /users`. Cách này đảm bảo trang "Vai trò" LUÔN khớp 100% với quyền thực thi thật của app, tự động cập nhật nếu sau này sửa `permissions.ts`, không cần nhớ sửa 2 nơi.
  - Đã verify (smoke): hiện đúng 4 thẻ vai trò với dữ liệu thật.
- **RBAC G2+G3 — test KỸ theo yêu cầu riêng (quyền nhạy cảm nhất hệ thống)**: đăng nhập lần lượt cả 3 tài khoản `manager.test`/`staff.test`/`viewer.test` → gõ thẳng URL `/users` và `/roles` → CẢ 3 vai trò đều bị chặn đúng ở route guard ("Không có quyền truy cập") lẫn ẩn đúng cả 2 mục trên sidebar — chỉ `admin` thấy và thao tác được. Không có trường hợp nào lọt qua.
- Không có `pageerror` ở bất kỳ kịch bản nào trong toàn bộ G1-G3.
- Ảnh hưởng tới: Web (Giai đoạn H — kiểm thử tổng cuối, cần test thủ công đủ 4 vai trò theo `H1`, đã có sẵn 3 tài khoản test + đã biết rõ hành vi từng trang qua toàn bộ Giai đoạn C-G để đối chiếu nhanh); không ảnh hưởng Mobile/AI Service (Giai đoạn G không có API/endpoint mới nào phát sinh, chỉ dùng lại các endpoint đã có).

## 2026-09-13 — Giai đoạn H: Kiểm thử & hoàn thiện — HOÀN TẤT WEB APP (A→H)
- Bối cảnh: rà soát tổng thể cuối cùng trước khi merge `feature/web-integration` vào `main`. Không phải rà từng trang riêng lẻ như các giai đoạn trước — đây là lượt quét CHÉO toàn bộ 14 trang × 4 vai trò + toàn bộ trang ở 1 độ phân giải cố định + tái xác nhận luồng phức tạp nhất từ đầu.
- **H1 (ma trận 4 vai trò × 14 trang)**: viết 1 script Playwright duy nhất lặp qua ĐỦ 4 tài khoản thật (`admin`, `manager.test`, `staff.test`, `viewer.test`) × ĐỦ 14 trang, kiểm tra 2 tầng cho mỗi ô: (a) mục có/không xuất hiện đúng trên sidebar, (b) gõ thẳng URL có bị chặn đúng bằng màn "Không có quyền truy cập" hay không — tổng **112 lượt kiểm tra (4×14×2), KẾT QUẢ 112/112 PASS**, không có ô nào sai lệch so với `config/permissions.ts`. Không lặp lại kiểm tra quyền GHI ở cấp nút (đã làm kỹ ở Giai đoạn C/C4/F/G cho từng trang khi xây), vì xét toàn bộ `RESOURCE_WRITE_ACCESS` hiện tại chỉ có ĐÚNG 2 trang có gap "xem được nhưng không ghi được" trong cùng 1 trang (`categories` — viewer đọc; `warehouses` — staff chỉ sửa vị trí không sửa kho), cả 2 đã verify ảnh chụp cụ thể từ trước; mọi trang nghiệp vụ F1-F6 và G1-G3 đều có `PAGE_ACCESS` trùng khớp `RESOURCE_WRITE_ACCESS` (không có role nào "xem nhưng không ghi") nên không tồn tại thêm gap nào để test.
- **H2 (responsive 1366×768)**: kiểm tra tự động (không chỉ nhìn ảnh chụp bằng mắt) bằng cách so `document.documentElement.scrollWidth` với `clientWidth` (dung sai 2px) — chạy cho ĐỦ 14 trang ở view danh sách mặc định VÀ 5 form/modal có bố cục nhiều cột nhất (dễ vỡ layout nhất): `TransferPage` (grid 5 cột nguồn-mũi tên-đích), `ExportPage` (grid 3 cột), `ImportPage` bước 1 AI (grid 2 cột + ảnh), modal "Tạo phiên kiểm kê", modal "Thêm người dùng". Kết quả: **0/19 vỡ layout** (không phát sinh thanh cuộn ngang ở màn hình nào).
- **H3 (luồng AI end-to-end, chạy lại từ đầu chứ không chỉ tin lại kết quả F1)**: dùng lại chính xác luồng đã verify ở F1 nhưng chạy MỚI sau khi đã hoàn tất F2 đến G3 (nhiều thay đổi code xen giữa: thêm 6 trang nghiệp vụ khác + Lịch sử + Người dùng + Vai trò) để phát hiện regression nếu có — upload file fixture thật → `POST /ai/detect` → map 2 nhóm (resistor/ic_chip) sang `TEST-RELAY`/`TEST-IC555` + vị trí `A1-TEST` → `POST /import-orders/from-ai` → verify qua `GET /inventory` tăng đúng (+5/+2). Không phát hiện regression nào — luồng vẫn hoạt động 100% chính xác như lúc mới viết ở F1.
- **H4 (audit token màu — không phải rà bằng mắt từng trang mà audit bằng công cụ, chính xác hơn)**: `grep` toàn bộ `web/src/{pages,components,layouts,contexts,hooks}` tìm (a) hex trùng đúng 8 giá trị đã khai báo trong `tokens.css` xuất hiện dạng chuỗi cứng thay vì qua class Tailwind sinh từ token — **0 kết quả**; (b) cú pháp Tailwind arbitrary-value màu `bg-[#...]`/`text-[#...]`/`border-[#...]` — **0 kết quả**; (c) `style={{...}}` nội tuyến chứa mã hex — **0 kết quả**. 2 file `07-DECISIONS-LOG.md` (Giai đoạn B) từng ghi chú "còn hex cứng sót lại có chủ đích, hẹn dọn ở H4" là `LoginPage.tsx` và `SuppliersPage.tsx` — kiểm tra lại xác nhận CẢ HAI đã được dọn sạch từ trước (LoginPage ở Giai đoạn C2 khi viết lại luồng đăng nhập thật, SuppliersPage ở Giai đoạn E5 khi nối API thật) — không còn việc gì phải làm ở H4 cho 2 file này.
  - Làm rõ 1 điểm dễ hiểu nhầm: các màu Tailwind mặc định dùng làm điểm nhận diện cho từng LOẠI dữ liệu (VD mã phiếu Nhập kho tô `text-indigo-600`, Xuất kho `text-violet-600`, Chuyển kho `text-blue-600`, Thu hồi `text-purple-600`, badge 7 màu của `HistoryPage.typeConfig`...) KHÔNG PHẢI vi phạm quy tắc token — đây là quy ước phân biệt trực quan giữa các loại nghiệp vụ/trạng thái, đã được xác nhận là chủ ý và giữ nguyên từ Giai đoạn B (`Badge` 7 màu, xem mục Giai đoạn B ở trên). Token trong `tokens.css` chỉ dành cho ĐÚNG 8 màu ngữ nghĩa/thương hiệu dùng xuyên suốt toàn app (gradient brand, danger/warning/success/info/accent, nền/viền) — không phải toàn bộ màu sắc xuất hiện trong app.
- **H5 (tổng kết cuối — mục này)**.
- **TỔNG KẾT TOÀN BỘ WEB APP (Giai đoạn A → H, khớp `08-WEB-BUILD-CHECKLIST.md`)**:
  - A: xoá "VAI TRÒ DEMO", 8 design token cố định trong `tokens.css`, cấu trúc thư mục + cài đặt (`react-router-dom`/`axios`/`@tanstack/react-query`).
  - B: audit toàn bộ component dùng chung (`components/ui.tsx`), phát hiện+sửa bug Rules of Hooks thật ở `ImportPage.tsx`, phát hiện lệch màu Badge "Hoàn tất" ở `DisposalPage.tsx`, phát hiện quan trọng OrderStatus thật chỉ có 3 giá trị (không phải 5 như mock).
  - C: App Shell + Auth thật (JWT interceptor + 401 tự bounce login) + `PAGE_ACCESS`/`RESOURCE_WRITE_ACCESS` 2 tầng phân quyền + route guard `PagePermission`.
  - D: Dashboard nối API thật (stats, biểu đồ 7 ngày, hoạt động gần đây dùng `signedQuantity()`).
  - E: CRUD thật cho toàn bộ danh mục/vật tư/kho/vị trí/NCC/tồn kho (6 resource), phát hiện đúng semantics soft-delete vs FK RESTRICT/SET NULL qua đọc trực tiếp `migration.sql`.
  - F: 6 nghiệp vụ kho (Nhập-có-AI/Xuất/Chuyển/Thu hồi/Kiểm kê/Thanh lý) nối API thật 100%, đúng transaction/rollback thật, kịch bản "nhiễu" cho Kiểm kê đã chứng minh đúng hành vi SET tuyệt đối.
  - G: Lịch sử biến động kho (giữ đúng thứ tự asc theo quyết định riêng) + Quản lý người dùng (CRUD thật) + Quản lý vai trò (đọc trực tiếp từ `permissions.ts`, không mô tả tay).
  - H: quét chéo toàn diện 4 vai trò × 14 trang (112/112 pass), responsive 1366×768 (0/19 vỡ layout), luồng AI end-to-end tái xác nhận không regression, audit token màu bằng công cụ (0 vi phạm).
  - **6 yêu cầu gốc của người dùng (đầu phiên) đã hoàn thành đủ cả 6**: (1) xoá "VAI TRÒ DEMO", dùng role thật từ `/auth/me`; (2) toàn bộ mock đã thay bằng API thật theo `02-BACKEND-SPEC.md`; (3) màu sắc gom về 1 file `tokens.css` duy nhất (8 token) — các màu điểm nhấn theo loại dữ liệu là quy ước riêng, không phải token, đã làm rõ ở H4; (4) đã chọn 1 phương án điều hướng duy nhất (React Router, có lý do trong Giai đoạn C) và áp dụng xuyên suốt; (5) đã audit đủ 14 trang so với `03-WEB-SPEC.md`/checklist, không còn thiếu trang nào; (6) JWT interceptor + tự động redirect login khi hết hạn — đã có từ Giai đoạn C, verify lại ở H1.
  - **Việc CHƯA làm, có chủ đích (không phải bỏ sót)**: nút "Huỷ phiếu" (trạng thái CANCELLED) cho 6 nghiệp vụ kho — Backend chưa có endpoint hỗ trợ (ghi nhận ở Giai đoạn F, người dùng đã xác nhận để dành hướng phát triển sau).
  - Build production (`npm run build` = `vite build`) sạch — 0 lỗi TypeScript (`tsc --noEmit`), chỉ có 1 cảnh báo không nghiêm trọng từ Vite ("chunk lớn hơn 500kB, cân nhắc code-splitting") — không phải lỗi, không chặn merge, có thể tối ưu sau nếu cần.
- Ảnh hưởng tới: Mobile App (Giai đoạn tiếp theo, dùng lại đúng `02-BACKEND-SPEC.md` + toàn bộ quyết định trong file này — đặc biệt: 3 trạng thái phiếu thật, cấu trúc `POST /ai/detect`, RBAC 2 nhóm role, semantics SET tuyệt đối của Kiểm kê); AI Service (giao diện `/api/ai/detect` đã CHỐT CỨNG, khi thay mock bằng AI thật phải giữ đúng cấu trúc response `{detections, summary, annotated_image}` đã ghi ở Giai đoạn F1).
